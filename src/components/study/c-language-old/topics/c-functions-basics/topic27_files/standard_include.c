#include <stdio.h>   // standard I/O
#include <stdlib.h>  // standard library (malloc, free, etc.)
#include <math.h>    // mathematical functions (if compiled with -lm)

int main() {
    printf("Hello from standard includes!\n");
    return 0;
}