const topic4_questions = [
  {
    "question": "What flags are attached to a synthetic bridge method in Java bytecode when inspected with 'javap -v'?",
    "shortAnswer": "In the disassembled classfile header generated by 'javap -v', a bridge method is tagged with two specific access flags: 'ACC_BRIDGE' (marking it as an artificial method generated for generic overriding) and 'ACC_SYNTHETIC' (marking that it was generated automatically by the compiler and not written by the developer).",
    "explanation": "Standard JVM classfile format specification (JVMS 4.7.15).",
    "hint": "Tagged with 'ACC_BRIDGE' and 'ACC_SYNTHETIC' flags in the classfile constant pool.",
    "level": "Advanced",
    "codeExample": "flags: (0x1041) ACC_PUBLIC, ACC_BRIDGE, ACC_SYNTHETIC"
  }
];

export default topic4_questions;