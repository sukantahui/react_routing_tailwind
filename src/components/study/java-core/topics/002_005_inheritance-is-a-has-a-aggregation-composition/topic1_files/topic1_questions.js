const topic1_questions = [
  {
    question: "What are the common synonyms used for parent and child classes in OOP?",
    shortAnswer: "Parent = Superclass = Base Class. Child = Subclass = Derived Class = Extended Class.",
    explanation: "Java uses the term 'superclass' for the parent and 'subclass' for the child.",
    hint: "Super/Base class (parent) and Sub/Derived class (child).",
    level: "Beginner",
    codeExample: "class Sub extends Super {}"
  }
];

export default topic1_questions;