const topic5_questions = [
  {
    question: "What is the role of 'java.lang.Object' in the Java class hierarchy?",
    shortAnswer: "'java.lang.Object' is the cosmic root superclass of every single class in Java. If a class does not explicitly extend another class, the compiler automatically adds 'extends Object'.",
    explanation: "It supplies 11 core methods (e.g. toString(), equals(), hashCode(), getClass(), clone(), wait(), notify()) to all objects in the Java universe.",
    hint: "The root superclass of all Java classes providing 11 fundamental methods.",
    level: "Beginner",
    codeExample: "Object obj = new Trainee(); // Always valid"
  }
];

export default topic5_questions;