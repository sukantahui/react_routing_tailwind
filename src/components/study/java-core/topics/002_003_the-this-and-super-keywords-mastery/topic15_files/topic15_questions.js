const topic15_questions = [
  {
    question: "Summarize the core differences between 'this' and 'super' in Java.",
    shortAnswer: "'this' refers to the current class instance, resolves shadowing, invokes peer methods, and chains peer constructors via 'this()'. 'super' refers to the direct parent class, accesses shadowed parent variables, invokes overridden parent methods, and chains parent constructors via 'super()'.",
    explanation: "Both are fundamental reference variables in Java OOP enabling clean class-level encapsulation and inheritance hierarchies.",
    hint: "Current class instance ('this') vs direct parent class members ('super').",
    level: "Beginner",
    codeExample: "// Complete comparison table in Topic 15"
  }
];

export default topic15_questions;