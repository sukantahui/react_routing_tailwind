const topic5_questions = [
  {
    "question": "Summarize the primary behavioral differences among List, Set, Queue, and Map in the Java Collections Framework.",
    "shortAnswer": "1. 'List': Ordered sequence accessed by numeric index; permits duplicate elements. 2. 'Set': Mathematical set containing strictly unique elements; forbids duplicates. 3. 'Queue/Deque': Collection designed for holding elements prior to processing (FIFO queue, LIFO stack, or Priority heap). 4. 'Map': Key-Value dictionary where each unique key maps to exactly one value.",
    "explanation": "The foundational taxonomy of all Java data structures.",
    "hint": "List = ordered/duplicates; Set = unique/no duplicates; Queue = processing buffer; Map = key-value pairs.",
    "level": "Beginner",
    "codeExample": "List<T> list; Set<T> set; Queue<T> queue; Map<K, V> map;"
  }
];

export default topic5_questions;