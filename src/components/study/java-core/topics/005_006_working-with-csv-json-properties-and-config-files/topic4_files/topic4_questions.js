const topic4_questions = [
  {
    "question": "What automatic comments does 'properties.store(Writer writer, String comments)' write at the top of the generated .properties file?",
    "shortAnswer": "1. The descriptive comment string supplied as the second parameter (prefixed with '# '). 2. An automatic second comment line containing the exact current date and timestamp generated by the JVM.",
    "explanation": "Standard persistence mechanism for Java desktop preferences and server properties.",
    "hint": "Writes the custom comment header followed by an auto-generated current timestamp comment line.",
    "level": "Beginner",
    "codeExample": "props.store(writer, \"Application Database Config\");"
  }
];

export default topic4_questions;