const topic6_questions = [
  {
    question: "Can static methods be overloaded in Java?",
    shortAnswer: "Yes! Java allows multiple static methods with the same name in the same class as long as their parameter lists (types, count, or order) differ.",
    explanation: "Static method overloading is resolved statically at compile time using static method signatures.",
    hint: "Yes, static methods can be overloaded based on different parameter signatures.",
    level: "Beginner",
    codeExample: "static void print(int a) {}\nstatic void print(String s) {}"
  }
];

export default topic6_questions;