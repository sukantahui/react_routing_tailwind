const topic1_questions = [
  {
    question: "What is an Abstract Class in Java and how is it declared?",
    shortAnswer: "A class declared with the 'abstract' keyword ('public abstract class ClassName { ... }') that serves as an incomplete template for subclasses and cannot be directly instantiated.",
    explanation: "Can contain both abstract methods (without bodies) and concrete methods (with bodies).",
    hint: "Declared with 'abstract' modifier; incomplete template that cannot be instantiated directly.",
    level: "Beginner",
    codeExample: "public abstract class BaseService { public abstract void execute(); }"
  }
];

export default topic1_questions;