const topic6_questions = [
  {
    question: "Can an interface in Java extend more than one interface using the 'extends' keyword?",
    shortAnswer: "Yes! While classes can only extend ONE superclass, an interface can extend MULTIPLE interfaces simultaneously ('interface C extends A, B').",
    explanation: "This allows combining smaller interfaces into composite contracts.",
    hint: "Interfaces support multiple inheritance via 'interface C extends A, B'.",
    level: "Beginner",
    codeExample: "interface Duplex extends Readable, Writable {}"
  }
];

export default topic6_questions;