const topic6_questions = [
  {
    question: "What is a Covariant Return Type in Java (introduced in Java 5)?",
    shortAnswer: "The ability of an overriding subclass method to specify a return type that is a subtype (subclass) of the return type declared in the superclass method.",
    explanation: "Eliminates the need for explicit type casting on the caller side when working with specialized factories.",
    hint: "Subclass method returns a more specific subtype than the parent method.",
    level: "Intermediate",
    codeExample: "// Parent: Person get()\n// Child: TraineeDeveloper get()"
  }
];

export default topic6_questions;