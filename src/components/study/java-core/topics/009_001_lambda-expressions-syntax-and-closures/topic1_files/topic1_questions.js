const topic1_questions = [
  {
    "question": "Describe the three core structural components of a Java Lambda Expression.",
    "shortAnswer": "A lambda expression consists of three components: 1. 'Parameters' (Argument list in parentheses, e.g. '(double amount, double rate)' or '(a, b)'). 2. 'Arrow Token' ('->', known as the lambda operator, separating arguments from execution logic). 3. 'Body' (either a single expression or a curly-brace code block '{ ... }' containing the statements to execute and return).",
    "explanation": "Syntax grammar of Java 8 Lambda Expressions (JLS §15.27).",
    "hint": "(parameter list) -> { body block / expression }",
    "level": "Beginner",
    "codeExample": "(a, b) -> a + b"
  }
];

export default topic1_questions;