const topic7_questions = [
  {
    question: "List the 4 Access Modifiers in Java in increasing order of visibility/accessibility.",
    shortAnswer: "1. private (Same Class only) < 2. default / package-private (Same Package) < 3. protected (Same Package + Subclasses in other packages) < 4. public (Anywhere).",
    explanation: "Default access has no keyword and is sometimes called package-private.",
    hint: "private < default < protected < public.",
    level: "Beginner",
    codeExample: "private int a; int b; protected int c; public int d;"
  }
];

export default topic7_questions;