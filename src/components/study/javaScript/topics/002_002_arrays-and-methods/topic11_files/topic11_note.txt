================================================================================
CODER & ACCOTAX - JAVASCRIPT COMPLETE ROADMAP
MODULE 002_002_ARRAYS-AND-METHODS: ARRAYS & ESSENTIAL ARRAY METHODS
TOPIC 11: MODERN IMMUTABLE ARRAY METHODS (TOSORTED, TOREVERSED, TOSPLICED, WITH)
Educator: Sukanta Hui | Barrackpore, West Bengal, India
================================================================================

1. TOPIC OVERVIEW & DETAILED CONCEPT DISCUSSION
--------------------------------------------------------------------------------
- Definition: Write pure, immutable array code with ES2023 additions: toSorted(), toReversed(), toSpliced(), and the with() single-index replacement method.
- Classroom Experience: Abhronila updated a user list in a React state hook using `users.reverse()` and state failed to re-render because the array reference did not change.
- Solution: Sukanta Hui showed how `toReversed()` returns a brand new array copy without mutating the original, triggering clean React re-renders.
- Core Invariant: Mastering array methods ensures high-performance, immutable data processing.

2. IN-DEPTH TECHNICAL KNOW-HOW & ENGINE RUNTIME MECHANICS
--------------------------------------------------------------------------------
- Execution Context Lifecycle: Memory allocation and element kind tracking in V8.
- Memory Stack vs Heap: Continuous array buffer vs hash table dictionary mode.
- Abstract Operations: ToPrimitive, SameValueZero, and Length invariants.
- V8 Optimizations: Fast elements, monomorphic inline caching, and TimSort.

3. EXCEPTIONS, CORNER CASES, COERCIONS & QUIRKS
--------------------------------------------------------------------------------
[!] RangeError on with() Out of Bounds: `arr.with(10, 'x')` throws `RangeError: Invalid index` if index is out of bounds.
[!] Shallow Copy Guarantee: These methods shallow copy objects; nested objects still share memory references.
[!] Browser Compatibility & Polyfills: ES2023 features require Node.js 20+ or modern browsers (or core-js polyfills).

4. 💎 SPECIAL LANGUAGE FEATURES & SENIOR PRO SECRETS
--------------------------------------------------------------------------------
- Secret 1: Clean React State Updating with Array.prototype.with()
  Replace cumbersome spread-slice patterns with clean, expressive `with()` calls.
- Secret 2: Prevent array de-optimization by avoiding delete operations on indices.
- Secret 3: Leverage ES2023 immutable methods for clean state management.

5. PRACTICAL ENTERPRISE USE-CASES & CODE EXAMPLES
--------------------------------------------------------------------------------
Example 1: Fundamental Mechanics
console.log("Fundamental array pattern executed");

Example 2: Data Pipeline & Transformation
const result = [10, 20, 30].map(x => x * 1.18);

Example 3: Defensive Boundary Validation
// Defensive validation guards

Example 4: Real-World Enterprise Workflow
// Enterprise dataset filtering and ranking

Example 5: Senior Pro Pattern
const todos = [
  { id: 1, title: "Learn V8 Internals", done: false },
  { id: 2, title: "Master ES2023", done: false }
];
// Toggle todo at index 0 immutably
const updatedTodos = todos.with(0, { ...todos[0], done: true });
console.log(todos[0].done); // false (Unchanged!)
console.log(updatedTodos[0].done); // true (New copy!)

6. COMPARATIVE SPECIFICATION CHEAT-SHEET
--------------------------------------------------------------------------------
+----------------------+--------------------+---------------------+--------------------+
| Feature / Operation  | Syntax / Signature | Return Type / State | Common Gotcha      |
+----------------------+--------------------+---------------------+--------------------+
| arr.toSorted(comparator) | Returns a new sort | Allocates copy then | React state & Redu |
| arr.toReversed()     | Returns a new reve | Allocates copy then | Chronological data |
| arr.toSpliced(start, count, ...items) | Returns a new arra | Allocates copy with | Immutable list upd |
| arr.with(index, value) | Returns a new arra | Allocates copy with | Single item state  |
+----------------------+--------------------+---------------------+--------------------+

7. INTERVIEW & VIVA QUICK-FIRE KNOWLEDGE POINTS
--------------------------------------------------------------------------------
Q1: What is the primary role of Modern Immutable Array Methods (toSorted, toReversed, toSpliced, with)?
A1: Write pure, immutable array code with ES2023 additions: toSorted(), toReversed(), toSpliced(), and the with() single-index replacement method.

Q2: How does V8 optimize array memory for this operation?
A2: By maintaining contiguous packed element buffers and using fast C++ native methods.

Q3: What common bug occurs with junior developers?
A3: Using Mutating Methods in React State Hooks: `state.sort()` mutates state directly and breaks change detection.

Q4: What is the Senior Pro recommended pattern?
A4: Use ES2023 Immutable Methods: Return fresh references directly without manual cloning.
================================================================================
