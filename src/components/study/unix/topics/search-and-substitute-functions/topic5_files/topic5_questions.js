const questions = [
  { question: "What is the purpose of using different delimiters in sed?", shortAnswer: "To avoid escaping the delimiter character inside the pattern or replacement.", explanation: "Any character can follow the `s` command, e.g., `s|pattern|replacement|`. This is especially useful when your pattern contains slashes, like file paths.", hint: "Pick a delimiter that does not appear in the pattern or replacement.", level: "intermediate", codeExample: "sed 's|/home/user|/home/guest|' file" },
  { question: "Which delimiters are commonly used besides `/`?", shortAnswer: "`|`, `#`, `@`, `%`, `^`, `!`, `:`, `;`, `,`", explanation: "You can use any printable character except backslash and newline. Choose one that doesn't conflict with your text.", hint: "Avoid using the delimiter inside pattern unless you escape it.", level: "basic", codeExample: "sed 's#pattern#replacement#'" },
  { question: "What does the `g` flag do?", shortAnswer: "Replaces all occurrences on each line, not just the first.", explanation: "Without `g`, sed stops after the first match on a line. With `g`, it continues to the end of the line.", hint: "Think of `g` as 'global' or 'all'.", level: "basic", codeExample: "echo 'a a a' | sed 's/a/b/g'   # b b b" },
  { question: "How does the `i` flag work?", shortAnswer: "Makes the pattern matching case‑insensitive.", explanation: "`i` ignores uppercase/lowercase differences when matching the pattern.", hint: "Useful for log files where 'Error' and 'error' may appear.", level: "basic", codeExample: "sed 's/error/BUG/i' log.txt" },
  { question: "What does the `p` flag do and when is it used?", shortAnswer: "Prints the line if a substitution was made. Usually used with `-n`.", explanation: "`p` alone prints the line regardless; with `-n` it prints only the changed lines.", hint: "Useful for debugging or extracting transformed lines.", level: "intermediate", codeExample: "sed -n 's/pattern/replacement/p' file" },
  { question: "How do you write only changed lines to a separate file?", shortAnswer: "Use the `w filename` flag.", explanation: "Every line where substitution occurs is appended to the given file.", hint: "The file is created/overwritten before sed starts, then appended.", level: "advanced", codeExample: "sed 's/old/new/w output.txt' input.txt" },
  { question: "What does a numeric flag like `2` do?", shortAnswer: "Replaces only the Nth occurrence on each line.", explanation: "`2` means the second match, `3` the third, etc. Number must be positive.", hint: "Use `2g` to replace from the second onward.", level: "advanced", codeExample: "echo 'a a a a' | sed 's/a/b/2'   # a b a a" },
  { question: "Can you use both a number and `g` together? What does `2g` mean?", shortAnswer: "Yes, `2g` replaces the second and all subsequent occurrences.", explanation: "`2g` means start at the second match and replace through the end of line.", hint: "Order matters: `s/old/new/2g` works, `s/old/new/g2` does not.", level: "expert", codeExample: "echo 'a a a a' | sed 's/a/b/2g'   # a b b b" },
  { question: "What is the `e` flag (GNU sed only)?", shortAnswer: "Executes the replacement as a shell command and replaces with its output.", explanation: "The replacement string is run as a command; the command's stdout becomes the replacement.", hint: "Powerful but dangerous – ensure the replacement is sanitised.", level: "expert", codeExample: "echo '5' | sed 's/[0-9]+/echo $((&*2))/e'   # 10 (GNU)" },
  { question: "What does the `I` flag do (uppercase i)?", shortAnswer: "Same as `i` – case‑insensitive. Some GNU sed versions support both.", explanation: "`I` may be required on some systems for case‑insensitivity.", hint: "Use `i` for portability unless you need `I`.", level: "intermediate", codeExample: "sed 's/error/BUG/I' log.txt" },
  { question: "Can I combine multiple flags? Example: global and case‑insensitive?", shortAnswer: "Yes, combine them as `gi` or `ig`.", explanation: "Flags can be in any order (except number before `g`).", hint: "Common combos: `gi`, `2g`, `pi` (print + case‑insensitive).", level: "intermediate", codeExample: "sed 's/error/BUG/gi' log.txt" },
  { question: "What happens if I use `w` flag but the file already exists?", shortAnswer: "sed overwrites the file before processing, then appends as it goes.", explanation: "If the file exists, its content is truncated (emptied) at the start.", hint: "To append without truncating, use shell redirection instead: `>>`.", level: "advanced", codeExample: "# Each sed run will erase previous output" },
  { question: "How to use extended regular expressions with flags?", shortAnswer: "Pass `-E` before the script, then use regex like `+`, `?`, `{n}`, `|`, `()` without backslashes.", explanation: "Flags like `g`, `i` work the same with `-E`.", hint: "`-E` is not a flag of the `s` command but a command‑line option.", level: "advanced", codeExample: "sed -E 's/[0-9]+/NUM/g' file" },
  { question: "What is the difference between `-E` and using `\\+` in basic regex?", shortAnswer: "Basic regex requires `\\+` for one-or-more, extended regex uses `+`.", explanation: "`-E` makes patterns more readable and matches modern regex engines.", hint: "Always use `-E` unless you need POSIX portability to very old systems.", level: "intermediate", codeExample: "# Basic: sed 's/[0-9]\\+/NUM/g'\n# Extended: sed -E 's/[0-9]+/NUM/g'" },
  { question: "Can I use the `p` flag with `-i` to see which lines changed while editing in‑place?", shortAnswer: "No, `-i` suppresses output, and `p` won't print to terminal. Use `-i.bak` and examine backup.", explanation: "To see changes, run without `-i` first.", hint: "Combine `-n` with `p` to preview before in‑place edit.", level: "advanced", codeExample: "sed -n 's/old/new/p' file   # preview" },
  { question: "How to replace the last occurrence of a pattern on each line?", shortAnswer: "Use a numeric flag with a very large number, e.g., `99` if line has ≤99 occurrences.", explanation: "Better: reverse the line, replace first, reverse back (complex).", hint: "For exact last: use Perl: `perl -pe 's/.*\\Kold/new/'`.", level: "expert", codeExample: "sed 's/.*\\(old\\)/\\1new/'   # not perfect" },
  { question: "What does the `M` flag do (GNU sed)?", shortAnswer: "Multiline mode: makes `^` and `$` match after/before newlines inside pattern space.", explanation: "Used with `-z` or when pattern space contains newlines.", hint: "Advanced; rarely needed. Use `-z` to slurp whole file.", level: "expert", codeExample: "sed -z 's/^/START\\n/M' file" },
  { question: "How to suppress the default output and only print when substitution occurs?", shortAnswer: "Use `-n` together with the `p` flag.", explanation: "`-n` turns off auto‑printing; `p` prints the line only if a change was made.", hint: "Great for extracting lines that match a complex pattern.", level: "intermediate", codeExample: "sed -n 's/^[0-9]\\+/NUM/p' file" },
  { question: "What happens if you use `w` flag with `-i`?", shortAnswer: "The `w` flag writes to a file separately; `-i` modifies the original. They are independent.", explanation: "You can have both: `sed -i 's/old/new/w changelog.txt' file` – original changed, changelog gets each changed line.", hint: "Useful for auditing.", level: "advanced", codeExample: "sed -i.bak 's/error/CRITICAL/w errors.log' app.log" },
  { question: "How to replace only if the pattern is found at the beginning of a line?", shortAnswer: "Use `^` anchor: `s/^pattern/replacement/`. Flags like `g` still work.", explanation: "Addressing or anchor inside pattern both work.", hint: "Combine with `-n` and `p` to see what matches.", level: "basic", codeExample: "sed 's/^ERROR/CRITICAL/' log.txt" },
  { question: "Can I use a newline as delimiter? Why not?", shortAnswer: "No, newline is not allowed as delimiter because sed commands are line‑oriented.", explanation: "The delimiter must be a single character other than backslash or newline.", hint: "Use any other visible character.", level: "advanced", codeExample: "# Not possible" },
  { question: "What does the `e` flag do with multiple lines?", shortAnswer: "Each line's replacement is executed as a separate command; output lines are inserted.", explanation: "The command's entire output becomes the replacement; may span multiple lines.", hint: "Use with `-z` to handle multiline commands.", level: "expert", codeExample: "# Not recommended for beginners" },
  { question: "How to make `w` flag append instead of overwrite?", shortAnswer: "Cannot; the `w` flag always overwrites (truncates) on each sed run. Use shell redirection for appending.", explanation: "Example: `sed 's/old/new/g' file >> output.txt` appends all lines.", hint: "For append, use `>>` outside sed.", level: "advanced", codeExample: "sed -n 's/old/new/p' file >> changes.log   # appends" },
  { question: "Is there a flag to replace only if the line matches another pattern (conditional)?", shortAnswer: "No flag, but you can use pattern address: `/pattern/ s/old/new/flags`.", explanation: "Addressing is more powerful than flags for conditionals.", hint: "Combine addresses with flags: `/ERROR/ s/old/new/g'", level: "intermediate", codeExample: "sed '/^ERROR/ s/old/new/gi' log.txt" },
  { question: "What does the `s` command's `NUMBERg` do when NUMBER is larger than occurrences?", shortAnswer: "If NUMBER exceeds the number of matches, nothing is replaced.", explanation: "Example: `s/old/new/5g` on a line with only 3 matches does nothing.", hint: "Safe; no error.", level: "advanced", codeExample: "echo 'a a' | sed 's/a/b/3g'   # unchanged" },
  { question: "How to replace the first occurrence globally across the whole file (not per line)?", shortAnswer: "Standard sed cannot do cross‑line first occurrence easily. Use `awk` or `perl -pe '...'`.", explanation: "You can slurp with `-z` and then replace first occurrence: `sed -z 's/old/new/'` replaces first in whole file.", hint: "`-z` treats file as one string.", level: "expert", codeExample: "sed -z 's/old/new/' file   # replaces first in whole file" },
  { question: "What is the purpose of the `#` delimiter? Does it create a comment?", shortAnswer: "`#` works as a delimiter, not a comment. Example: `sed 's#pattern#replacement#'`.", explanation: "Be careful: using `#` inside a script can confuse if not quoted properly.", hint: "If your pattern contains `#`, escape it or choose another delimiter.", level: "intermediate", codeExample: "sed 's#/usr/local#/opt#' file" },
  { question: "Can flags be used with other sed commands like `y` (transliterate)?", shortAnswer: "No, flags are only for the `s` command. `y` has its own syntax.", explanation: "`y/abc/123/` transforms characters, no flags.", hint: "Use `y` for one‑to‑one character mapping.", level: "basic", codeExample: "echo 'abc' | sed 'y/abc/123/'   # 123" },
  { question: "How do I use the `p` flag with `-n` and also save the result to a file?", shortAnswer: "Redirect output: `sed -n 's/pattern/replacement/p' file > output.txt`.", explanation: "The `p` flag prints to stdout; redirect captures it.", hint: "Combine with `-i`? Not possible – `-i` and `-n` together skip output entirely.", level: "intermediate", codeExample: "sed -n 's/error/CRITICAL/p' log.txt > errors_only.txt" },
  { question: "What is an example of using the `e` flag safely?", shortAnswer: "Use `e` to run a command that returns clean text, e.g., `date`, `basename`.", explanation: "Example: `echo 'file.txt' | sed 's/.*/basename &/e'` prints `basename file.txt` output `file.txt`.", hint: "Sanitise input to avoid command injection.", level: "expert", codeExample: "echo '/home/user/file' | sed 's#.*#basename &#e'   # file" }
];

export default questions;