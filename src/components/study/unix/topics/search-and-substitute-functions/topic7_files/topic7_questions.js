const questions = [
  { question: "What is the safest way to do a bulk find-and-replace across many files?", shortAnswer: "Use `grep -l` to list files first, preview changes, then use `xargs sed -i.bak` or a backup strategy.", explanation: "Always backup, test on one file, use version control, and dry-run with `grep -c` to count matches.", hint: "Never `sed -i` without first checking what will change.", level: "advanced", codeExample: "grep -l 'old' *.txt | xargs sed -i.bak 's/old/new/g'" },
  { question: "How can I rename multiple files by changing a pattern in their names?", shortAnswer: "Loop over files, use `sed` to generate new name, then `mv`.", explanation: "Example: `for f in *.txt; do mv \"$f\" \"$(echo $f | sed 's/old/new/')\"; done`", hint: "Test with `echo` before actual `mv`.", level: "intermediate", codeExample: "for f in report_*.txt; do mv \"$f\" \"${f/report/annual}\"; done  # bash parameter expansion" },
  { question: "How to update a database connection string in all `.env` files?", shortAnswer: "`sed -i 's/DB_HOST=localhost/DB_HOST=prod-db.example.com/' .env*`", explanation: "Use `-i` for in-place edit. Add `.bak` to create backups.", hint: "Backup first: `cp .env .env.bak`", level: "basic", codeExample: "sed -i.bak 's|localhost:5432|db.prod:5432|g' .env*" },
  { question: "What is the best way to clean a messy CSV with inconsistent quoting and spaces?", shortAnswer: "Use multiple `sed` passes: remove spaces around commas, then strip quotes, then normalize.", explanation: "Step-by-step approach is safer than a single complex regex.", hint: "Use `sed 's/ *, */,/g'` to clean spaces.", level: "advanced", codeExample: "sed -E 's/ *, */,/g; s/\"//g' messy.csv > clean.csv" },
  { question: "How to anonymize IP addresses in log files for GDPR compliance?", shortAnswer: "Use `sed -E` to replace the last octet with `.0` or `XXX`.", explanation: "Preserves the network part but hides host.", hint: "Example: `sed -E 's/([0-9]+\\.){3}[0-9]+/\\1XXX/g'`", level: "intermediate", codeExample: "sed -E 's/([0-9]{1,3}\\.){3}[0-9]{1,3}/\\10/g' access.log" },
  { question: "How can I convert a CSV file into SQL INSERT statements?", shortAnswer: "Use `sed` to transform each row into an INSERT line, or use `awk`.", explanation: "Skip header, then wrap each line with INSERT INTO ... VALUES (...).", hint: "Be careful with escaping single quotes in data.", level: "advanced", codeExample: "sed -n '2,$ s/^\\([^,]*\\),\\([^,]*\\),\\([^,]*\\)$/INSERT INTO t VALUES ('\\''\\1'\\'', '\\''\\2'\\'', '\\''\\3'\\'');/p' data.csv" },
  { question: "What is a safe way to remove all HTML tags from a file?", shortAnswer: "`sed 's/<[^>]*>//g'` works for simple cases, but for nested tags use a proper parser.", explanation: "HTML is not regular; sed is not a parser, but works for quick and dirty.", hint: "For production, use `pup` or `html-xml-utils`.", level: "advanced", codeExample: "sed 's/<[^>]*>//g; s/^[[:space:]]*//; s/[[:space:]]*$//' input.html" },
  { question: "How to batch convert Markdown frontmatter from YAML to TOML?", shortAnswer: "Use `sed` to replace `---` with `+++` and change key formats.", explanation: "Example: `sed -i 's/^title:/title = /' *.md`", hint: "Use `-i.bak` and test on a sample.", level: "expert", codeExample: "sed -i -E 's/^([a-z]+): /\\1 = \"/; s/$/\"/' frontmatter.md" },
  { question: "How to find and replace across a Git repository without touching `.git/`?", shortAnswer: "Use `git grep -l` to list files, then `xargs sed -i`.", explanation: "`git grep` automatically respects `.gitignore` and doesn't search inside `.git`.", hint: "Always commit before running to easily revert.", level: "advanced", codeExample: "git grep -l 'TODO' | xargs sed -i 's/TODO/FIXME/g'" },
  { question: "What is the most common mistake when doing production search/replace?", shortAnswer: "Not testing the command on a single file or dry-run first.", explanation: "A typo in regex can corrupt hundreds of files.", hint: "Always run `grep -c` to count matches before `sed -i`.", level: "basic", codeExample: "grep -c 'old' *.txt   # see how many matches" },
  { question: "How to replace text only in files that contain a specific pattern?", shortAnswer: "Use `grep -l` to filter files, then `xargs sed -i`.", explanation: "This avoids processing files that don't need changes, improving speed.", hint: "Combine: `grep -l 'pattern' *.log | xargs sed -i 's/old/new/'`", level: "intermediate", codeExample: "grep -l 'ERROR' *.log | xargs sed -i 's/ERROR/CRITICAL/g'" },
  { question: "How to insert a line before the first match in a file?", shortAnswer: "Use `sed '0,/pattern/s//newline\\n&/'`", explanation: "`0,/pattern/` finds the first match, then replaces with newline followed by matched line.", hint: "Backslash-escape newline.", level: "expert", codeExample: "sed '0,/ERROR/s//INSERTED\\n&/' log.txt" },
  { question: "How to delete all lines that do NOT contain a pattern?", shortAnswer: "`sed -n '/pattern/p'` or `sed '/pattern/!d'`", explanation: "The `!d` deletes lines not matching the pattern.", hint: "Equivalent to `grep 'pattern'`.", level: "basic", codeExample: "sed '/ERROR/!d' log.txt   # keep only ERROR lines" },
  { question: "How to append text to the end of every line that matches a pattern?", shortAnswer: "`sed '/pattern/s/$/ appended/'`", explanation: "`$` anchors at end of line; replacement appends text.", hint: "Use `s/$/.../` to add to all lines, or conditionally with address.", level: "intermediate", codeExample: "sed '/^ERROR/s/$/ - ALERT/' log.txt" },
  { question: "How to prepend a line number to every line in a file?", shortAnswer: "`sed = file | sed 'N;s/\\n/ /'`", explanation: "`=` prints line numbers, then `N` reads next line, `s/\\n/ /` joins them.", hint: "Simpler: `nl` or `cat -n`.", level: "advanced", codeExample: "sed = log.txt | sed 'N;s/\\n/ /' > numbered.log" },
  { question: "How to replace text only on lines between two patterns (excluding the pattern lines)?", shortAnswer: "Use `sed '/start/,/end/{/start/!{/end/! s/old/new/}}'`", explanation: "Nested addresses exclude the boundary lines.", hint: "Complex; often better to use `awk`.", level: "expert", codeExample: "sed '/<body>/,/<\\/body>/ {/<body>/!{/<\\/body>/! s/old/new/}}' page.html" },
  { question: "How to remove trailing whitespace from all files in a project?", shortAnswer: "`find . -type f -exec sed -i 's/[[:space:]]*$//' {} +`", explanation: "`[[:space:]]*$` matches spaces/tabs at line end.", hint: "Run on a backup first.", level: "intermediate", codeExample: "find . -name '*.py' -exec sed -i 's/[ \\t]*$//' {} \\;" },
  { question: "How to change all `require` statements to `import` in JavaScript files?", shortAnswer: "Complex: `sed -E 's/const\\s+(\\w+)\\s*=\\s*require\\(['\\'']([^'\\'']+)['\\'']\\);/import \\1 from '\\''\\2'\\'';/'`", explanation: "Capture variable name and module path, then reorder.", hint: "Use a tool like `jscodeshift` for reliable refactoring.", level: "expert", codeExample: "# Not recommended for production; use codemods" },
  { question: "How to convert Windows line endings (CRLF) to Unix (LF) with sed?", shortAnswer: "`sed -i 's/\\r$//' file`", explanation: "Removes carriage return at end of each line.", hint: "Also `dos2unix` utility.", level: "basic", codeExample: "sed -i 's/\\r$//' winfile.txt" },
  { question: "How to extract the second column from a CSV and replace its values?", shortAnswer: "Use `awk -F',' '{print $2}'` to extract, then pipe to sed.", explanation: "But to replace in-place, use `awk -F',' -v OFS=',' '{$2=\"new\"; print}'`", hint: "awk is better for column operations than sed.", level: "advanced", codeExample: "awk -F',' '{$2=\"NEW_VALUE\"; print}' OFS=',' data.csv > new.csv" },
  { question: "How to generate a report of all changes made by a sed command?", shortAnswer: "Use `sed -n 's/old/new/w changelog.txt' file` and also print to stdout.", explanation: "The `w` flag writes changed lines to a file; without `-n` it also prints all lines.", hint: "Add `-i` to modify original as well.", level: "advanced", codeExample: "sed -i -n 's/old/new/w changes.txt' file   # only changed lines written, original modified? No, -n suppresses output." },
  { question: "How to safely replace a string that contains slashes, dots, and stars?", shortAnswer: "Use a different delimiter in `sed`, e.g., `|` or `#`.", explanation: "Example: `sed 's|/usr/local|/opt|g'`", hint: "Or escape: `\\/usr\\/local`", level: "intermediate", codeExample: "sed 's#http://old.com#https://new.org#g' file" },
  { question: "What is the `-z` flag in GNU sed and when to use it?", shortAnswer: "Treats input as NUL-separated (i.e., whole file as one line). Useful for cross-line matches.", explanation: "With `-z`, `^` matches start of file, `$` matches end, and newlines are regular characters.", hint: "Use `-z` to replace first occurrence in file.", level: "expert", codeExample: "sed -z 's/old/new/' file   # replaces only first occurrence in whole file" },
  { question: "How to use sed with `-i` and also keep a timestamped backup?", shortAnswer: "`sed -i.bak_$(date +%Y%m%d_%H%M%S) 's/old/new/' file`", explanation: "The backup suffix is dynamic; ensures unique backup per run.", hint: "Store in a separate backup folder.", level: "advanced", codeExample: "backup_suffix=\".bak_$(date +%Y%m%d)\"; sed -i\"$backup_suffix\" 's/old/new/' file" },
  { question: "How to replace text in a specific column of a fixed-width file?", shortAnswer: "Use `sed` with `^.\{n\}` to skip columns.", explanation: "Example: to replace column starting at position 20 with length 10: `sed -E 's/^(.{19}).{10}/\\1NEWVALUE/'`", hint: "Use `cut` then paste for complex cases.", level: "expert", codeExample: "sed -E 's/^(.{19}).{10}/\\1XXXXXXXXXX/' fixed.txt" },
  { question: "What is the difference between `sed -i` and `sed > newfile`?", shortAnswer: "`-i` modifies the original file in-place; `>` creates/overwrites a new file.", explanation: "With `-i`, sed creates a temp file then renames; with redirection, you must manually rename.", hint: "Use `-i` for convenience, but be aware it can break symlinks.", level: "basic", codeExample: "# In-place: sed -i 's/old/new/' file\n# New file: sed 's/old/new/' file > newfile" },
  { question: "How to quickly swap two words in a file using sed?", shortAnswer: "Use temporary markers: `sed 's/old/tmp_swap/; s/new/old/; s/tmp_swap/new/' file`", explanation: "Three-step swap avoids using hold space.", hint: "Use `-E` and capture groups: `s/(old) (new)/\\2 \\1/`", level: "intermediate", codeExample: "echo 'apple banana' | sed -E 's/(apple) (banana)/\\2 \\1/'   # banana apple" },
  { question: "How to add a prefix to every line except the first line?", shortAnswer: "`sed '2,$ s/^/PREFIX: /' file`", explanation: "Range from line 2 to end, substitute start of line.", hint: "Also `sed '1! s/^/PREFIX: /'`", level: "basic", codeExample: "sed '2,$ s/^/  /' file   # indent all but first line" },
  { question: "How to comment out all lines containing a pattern?", shortAnswer: "`sed '/pattern/s/^/# /' file`", explanation: "Adds `# ` at beginning of matching lines.", hint: "To uncomment: `sed '/pattern/s/^# //' file`", level: "basic", codeExample: "sed '/ERROR/s/^/# /' log.txt" },
  { question: "What is a practical one-liner to find duplicate consecutive lines in a file?", shortAnswer: "`uniq` is better, but `sed '$!N; /^\\(.*\\)\\n\\1$/!P; D'`", explanation: "Advanced sed script using hold space pattern.", hint: "Use `uniq -d` instead for simplicity.", level: "expert", codeExample: "uniq -d file   # show duplicates" }
];

export default questions;