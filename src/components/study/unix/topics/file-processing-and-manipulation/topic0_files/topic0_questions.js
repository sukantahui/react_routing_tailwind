const questions = [
  { question: "What does `head -n 5 file` do?", shortAnswer: "Shows first 5 lines of the file.", explanation: "-n specifies the number of lines. Default is 10.", hint: "Negative values (head -n -5) show all but last 5 lines (GNU).", level: "basic", codeExample: "head -3 data.txt" },
  { question: "How to show the last 20 lines with `tail`?", shortAnswer: "`tail -20 file` or `tail -n 20 file`", explanation: "Both are valid; -n is optional for positive numbers.", hint: "Use `tail -n +2` to skip first line.", level: "basic", codeExample: "tail -20 log.txt" },
  { question: "What is the difference between `cut -b` and `cut -c`?", shortAnswer: "`-b` works on bytes, `-c` on characters (handles multi-byte correctly).", explanation: "For ASCII, they are identical. For UTF-8, `-c` respects character boundaries.", hint: "Use `-c` for human‑readable text.", level: "intermediate", codeExample: "cut -c 1-5 file" },
  { question: "How to extract the 2nd and 4th columns from a CSV file?", shortAnswer: "`cut -d',' -f2,4 file.csv`", explanation: "`-d` sets delimiter, `-f` lists fields.", hint: "No spaces after comma in `-f2,4`.", level: "basic", codeExample: "cut -d',' -f2,4 data.csv" },
  { question: "How to make `cut` use space as delimiter but handle multiple spaces?", shortAnswer: "Pipe through `tr -s ' '` first to squeeze spaces: `tr -s ' ' | cut -d' ' -f2`", explanation: "Cut treats each space as delimiter, so consecutive spaces create empty fields.", hint: "Alternative: `awk '{print $2}'` is easier.", level: "advanced", codeExample: "echo 'a    b    c' | tr -s ' ' | cut -d' ' -f2" },
  { question: "What does `tail -f` do?", shortAnswer: "Follows the file as it grows, outputting new lines in real time.", explanation: "Useful for monitoring log files. Press Ctrl+C to stop.", hint: "Combine with `grep` to filter live logs.", level: "intermediate", codeExample: "tail -f /var/log/syslog" },
  { question: "How to show a range of lines, e.g., lines 5-10?", shortAnswer: "Combine head and tail: `head -10 file | tail -6`", explanation: "First get first 10 lines, then take last 6 of those (lines 5-10).", hint: "Alternative: `sed -n '5,10p' file`.", level: "intermediate", codeExample: "head -10 data.txt | tail -6" },
  { question: "What is the default delimiter for `cut -f`?", shortAnswer: "Tab character.", explanation: "If you don't specify `-d`, cut splits on tabs.", hint: "Use `-d$'\\t'` explicitly if needed.", level: "basic", codeExample: "cut -f1 tab_separated.txt" },
  { question: "How to change the output delimiter in `cut`?", shortAnswer: "Use `--output-delimiter='string'`", explanation: "Replaces the input delimiter with the given string in the output.", hint: "Example: `cut -d',' -f1,3 --output-delimiter='|'`.", level: "intermediate", codeExample: "cut -d':' -f1,3 --output-delimiter=' ' /etc/passwd" },
  { question: "Why does `cut -d' ' -f2` fail on lines with multiple spaces?", shortAnswer: "Because `cut` treats each space as a delimiter, creating empty fields.", explanation: "To fix, pre‑process with `tr -s ' '` or use `awk`.", hint: "`awk '{print $2}'` ignores leading/trailing spaces.", level: "advanced", codeExample: "# cut fails: echo 'a  b  c' | cut -d' ' -f2\n# works: echo 'a  b  c' | tr -s ' ' | cut -d' ' -f2" },
  { question: "How to skip the header line before processing with `cut`?", shortAnswer: "Use `tail -n +2 file | cut ...`", explanation: "`tail -n +2` outputs from line 2 to end.", hint: "Combine with `head` if you also need first N rows after header.", level: "basic", codeExample: "tail -n +2 data.csv | cut -d',' -f2" },
  { question: "What is the difference between `cut -c1-` and `cut -c1-10`?", shortAnswer: "`-c1-` prints from character 1 to end of line; `-c1-10` prints first 10 characters.", explanation: "Omitted end means 'to the end'.", hint: "Similarly `-c-10` prints from start to character 10.", level: "basic", codeExample: "cut -c5- file   # from char5 to end" },
  { question: "How to select non‑contiguous columns with `cut`?", shortAnswer: "List them with commas: `-f1,3,5`", explanation: "You can also mix ranges: `-f1-3,5,7-9`.", hint: "Order doesn't matter; output appears in the order of fields as they appear in the line? Actually GNU cut respects the order you specify.", level: "intermediate", codeExample: "cut -d',' -f1,4,2 data.csv   # prints fields 1,4,2 in that order" },
  { question: "What does `head -n -0` do?", shortAnswer: "Outputs nothing (since removing 0 lines from end).", explanation: "Negative numbers remove lines from the end. `head -n -5` removes last 5 lines.", hint: "Not POSIX; GNU extension.", level: "advanced", codeExample: "head -n -0 file   # empty output" },
  { question: "How to use `cut` with pipe from `grep`?", shortAnswer: "`grep 'pattern' file | cut -d',' -f2`", explanation: "Pipe filters lines, then cut extracts columns.", hint: "Classic pipeline for log analysis.", level: "basic", codeExample: "grep 'ERROR' server.log | cut -d' ' -f4" },
  { question: "What is the maximum number of bytes `cut -b` can handle?", shortAnswer: "Line length is limited by system memory, but practically no hard limit.", explanation: "cut reads line by line, so memory per line is the constraint.", hint: "For huge lines, use `awk` or `sed`.", level: "advanced", codeExample: "# Works on any length line" },
  { question: "How to display both first 3 and last 3 lines of a file?", shortAnswer: "`(head -3; tail -3) < file` or `head -3 file; tail -3 file`", explanation: "Using a subshell with redirection reads the file only once.", hint: "Or just two separate commands.", level: "intermediate", codeExample: "(head -3; tail -3) < largefile.txt" },
  { question: "What is the difference between `tail -n +5` and `tail -n 5`?", shortAnswer: "`tail -n +5` starts from line 5 (skip first 4); `tail -n 5` shows last 5 lines.", explanation: "The plus sign changes meaning: `+K` starts at line K.", hint: "`tail -n +1` shows the whole file.", level: "intermediate", codeExample: "tail -n +5 data.txt   # lines 5 through end" },
  { question: "How to use `cut` with a delimiter that is a space but also preserve empty fields?", shortAnswer: "Cut cannot preserve consecutive delimiters. Use `awk -F' ' '{print $2}'` instead.", explanation: "Awk handles multiple delimiters by default.", hint: "If you must use cut, pre‑process with `sed 's/ /|/g'`.", level: "expert", codeExample: "echo 'a  b  c' | awk '{print $2}'   # prints 'b'" },
  { question: "What happens if you specify a field number that doesn't exist in a line?", shortAnswer: "cut prints nothing for that line (empty line).", explanation: "No error; missing fields result in empty output.", hint: "Use `grep` to filter lines with enough fields first.", level: "advanced", codeExample: "echo 'a,b' | cut -d',' -f3   # empty output" },
  { question: "How to use `head` and `tail` with multiple files?", shortAnswer: "`head -3 file1 file2` prints each file's first 3 lines with a header.", explanation: "The header line `==> file1 <==` helps distinguish.", hint: "Use `-q` to suppress headers.", level: "basic", codeExample: "head -2 *.txt" },
  { question: "Why does `cut -c 1-` sometimes output extra spaces?", shortAnswer: "Because the original line had trailing spaces/newline; cut preserves them.", explanation: "Cut does not trim whitespace.", hint: "Use `sed 's/[[:space:]]*$//'` to trim.", level: "intermediate", codeExample: "echo 'hello   ' | cut -c1- | cat -A   # shows trailing spaces" },
  { question: "How to select the last field of each line with `cut`?", shortAnswer: "You can't directly. Use `awk '{print $NF}'` or `rev | cut -d' ' -f1 | rev`.", explanation: "Cut doesn't support 'last field' notation.", hint: "Rev reverses line, cut first field, rev back.", level: "expert", codeExample: "echo 'a b c' | rev | cut -d' ' -f1 | rev   # prints 'c'" },
  { question: "What is the difference between `head -c 10` and `head -n 10`?", shortAnswer: "`-c` counts bytes/characters, `-n` counts lines.", explanation: "`head -c 10` prints first 10 bytes of the file, possibly part of a line.", hint: "Useful for binary files.", level: "advanced", codeExample: "head -c 100 file.bin" },
  { question: "How to show only the second line of a file using `head` and `tail`?", shortAnswer: "`head -2 file | tail -1`", explanation: "First get first 2 lines, then take the last of those (line 2).", hint: "Alternative: `sed -n '2p' file`.", level: "basic", codeExample: "head -2 data.txt | tail -1" },
  { question: "What is the performance of `cut` compared to `awk` for column extraction?", shortAnswer: "`cut` is generally faster for simple fixed delimiter extraction.", explanation: "Cut is lightweight, while awk is more powerful but slower.", hint: "For huge files, use cut when possible.", level: "expert", codeExample: "time cut -d',' -f2 big.csv > /dev/null" },
  { question: "How to use `cut` with a delimiter that is a colon and include multiple colons?", shortAnswer: "Same as any delimiter: `cut -d':' -f1,3 /etc/passwd`", explanation: "Colon is fine; no escaping needed.", hint: "Works for any single character except newline.", level: "basic", codeExample: "cut -d':' -f1 /etc/passwd   # usernames" },
  { question: "What does `tail -f file | grep --line-buffered 'pattern'` do?", shortAnswer: "Follows file and outputs lines matching pattern in real time.", explanation: "`--line-buffered` forces grep to flush output immediately.", hint: "Essential for live log monitoring.", level: "advanced", codeExample: "tail -f app.log | grep --line-buffered 'ERROR'" },
  { question: "How to extract columns from a file that uses multiple different delimiters?", shortAnswer: "Pre‑process with `sed` to standardise delimiters, then use `cut`.", explanation: "Example: `sed 's/[|;]/ /g' file | cut -d' ' -f2`", hint: "Better to use `awk` with regex field separator.", level: "expert", codeExample: "sed 's/[|;]/ /g' data.txt | cut -d' ' -f3" },
  { question: "What is the purpose of `--complement` in GNU cut?", shortAnswer: "Selects all fields/bytes/characters except those specified.", explanation: "Example: `cut -f2 --complement` prints all fields except field 2.", hint: "Useful for dropping columns.", level: "advanced", codeExample: "cut -d',' -f2 --complement data.csv   # remove column 2" }
];

export default questions;