const questions = [
  { question: "What does `uniq` do by default?", shortAnswer: "Removes adjacent duplicate lines (outputs only one copy of each consecutively repeated line).", explanation: "Only consecutive duplicates are removed; non‑adjacent duplicates remain.", hint: "Pipe through `sort` first to remove all duplicates.", level: "basic", codeExample: "uniq file.txt" },
  { question: "How to get only the lines that appear exactly once (unique lines)?", shortAnswer: "`uniq -u file.txt`.", explanation: "`-u` prints lines that are not repeated (appear only once in the input).", hint: "Input must be sorted for correct grouping.", level: "intermediate", codeExample: "sort file.txt | uniq -u" },
  { question: "What does `uniq -c` do?", shortAnswer: "Prefixes each output line with the number of occurrences (count).", explanation: "First column is the count, then the line itself.", hint: "Often piped to `sort -nr` to get highest frequency first.", level: "basic", codeExample: "sort file.txt | uniq -c" },
  { question: "How to show only duplicate lines (lines that appear more than once)?", shortAnswer: "`uniq -d file.txt`.", explanation: "`-d` prints each duplicate line once (the first occurrence of the duplicate block).", hint: "Useful to find repeated entries.", level: "intermediate", codeExample: "sort file.txt | uniq -d" },
  { question: "Why does `uniq` sometimes leave duplicates in the output?", shortAnswer: "Because the duplicates are not adjacent, or the file wasn't sorted.", explanation: "Uniq only compares consecutive lines; non‑adjacent duplicates are not removed.", hint: "Always `sort | uniq` for global deduplication.", level: "basic", codeExample: "# Wrong: uniq file; Right: sort file | uniq" },
  { question: "How to make uniq case‑insensitive?", shortAnswer: "Pre‑process with `tr '[:upper:]' '[:lower:]'` before sorting and uniq.", explanation: "Uniq has no built‑in case‑ignore option (unlike sort -f).", hint: "Example: `tr 'A-Z' 'a-z' < file | sort | uniq`.", level: "advanced", codeExample: "tr '[:upper:]' '[:lower:]' < file | sort | uniq" },
  { question: "What does `uniq -f N` do?", shortAnswer: "Skips the first N whitespace‑separated fields when comparing lines.", explanation: "Comparisons ignore the specified number of fields; the rest of the line determines uniqueness.", hint: "Field numbers start at 1; eg `-f2` skips two fields.", level: "advanced", codeExample: "uniq -f1 employees.txt" },
  { question: "What does `uniq -s N` do?", shortAnswer: "Skips the first N characters when comparing lines.", explanation: "Comparisons ignore the specified number of characters from the start of each line.", hint: "Useful for skipping line numbers or timestamps.", level: "advanced", codeExample: "uniq -s10 log.txt" },
  { question: "How to save the result of uniq directly to a file?", shortAnswer: "`uniq input.txt output.txt`.", explanation: "The second argument is the output file; if omitted, stdout is used.", hint: "Also redirection works: `uniq file.txt > out.txt`.", level: "basic", codeExample: "uniq sorted.txt result.txt" },
  { question: "Can uniq work on unsorted input if duplicates are adjacent?", shortAnswer: "Yes, but only those adjacent duplicates are removed; non‑adjacent duplicates will remain.", explanation: "Example: `a\\n a\\n b\\n a` becomes `a\\n b\\n a` (the last `a` remains).", hint: "For safety, always sort first.", level: "intermediate", codeExample: "uniq file.txt   # not safe" },
  { question: "How to count the number of unique lines in a file?", shortAnswer: "`sort file.txt | uniq | wc -l`.", explanation: "Sort, deduplicate, then count lines.", hint: "Or `sort -u file.txt | wc -l`.", level: "basic", codeExample: "sort data.txt | uniq | wc -l" },
  { question: "What is the difference between `sort -u` and `sort | uniq`?", shortAnswer: "`sort -u` is a single command that sorts and removes duplicates; `sort | uniq` pipes two commands. They produce identical output, but `sort -u` may be slightly more efficient.", explanation: "`sort -u` eliminates duplicates during sorting; `uniq` removes consecutive duplicates after sorting.", hint: "Use `sort -u` for simplicity and speed.", level: "intermediate", codeExample: "sort -u file.txt" },
  { question: "How to get the frequency of the most common line?", shortAnswer: "`sort file.txt | uniq -c | sort -nr | head -1`.", explanation: "Count, sort numerically descending, take first line.", hint: "Add `-k1,1nr` to sort only by count.", level: "advanced", codeExample: "sort file.txt | uniq -c | sort -nr | head -1" },
  { question: "Can uniq ignore whitespace differences between lines?", shortAnswer: "No; uniq considers lines with different whitespace as different. Use `sed` to normalise spaces first.", explanation: "Example: `sed 's/  */ /g' file | sort | uniq`.", hint: "Also `-w` (GNU) can compare only first N characters.", level: "expert", codeExample: "sed 's/[[:space:]]\\+/ /g' file | sort | uniq" },
  { question: "What does `uniq -w N` do (GNU extension)?", shortAnswer: "Compares only the first N characters of each line (ignores characters after position N).", explanation: "Useful when lines have trailing data that should be ignored.", hint: "Example: `uniq -w 10 file.txt`.", level: "advanced", codeExample: "uniq -w 5 short.txt" },
  { question: "How to find lines unique to fileA compared to fileB using uniq?", shortAnswer: "`cat fileA fileB | sort | uniq -u` shows lines that appear in only one of the two files.", explanation: "But it doesn't distinguish which file they came from; use `comm` for that.", hint: "`comm -23` is more precise.", level: "advanced", codeExample: "cat fileA fileB | sort | uniq -u" },
  { question: "Why does uniq sometimes output a space in front of counts with `-c`?", shortAnswer: "`uniq -c` right‑aligns counts with a leading space for formatting.", explanation: "Can be removed with `sed 's/^ *//'`.", hint: "Use `sed` to trim if needed for scripts.", level: "basic", codeExample: "uniq -c file.txt | sed 's/^ *//'" },
  { question: "How to treat multiple spaces as a single field separator in `-f`?", shortAnswer: "Uniq already treats any whitespace (spaces/tabs) as field separators; consecutive whitespace counts as a single delimiter.", explanation: "No special handling needed.", hint: "But leading spaces may affect field counting; use `sed` to strip.", level: "intermediate", codeExample: "uniq -f2 file.txt" },
  { question: "What happens if you use `-d` on a file with no duplicates?", shortAnswer: "No output (empty). Exit status is 0.", explanation: "`-d` only outputs lines that appear at least twice.", hint: "Check with `if` to see if any duplicates exist.", level: "basic", codeExample: "uniq -d file.txt && echo 'has duplicates'" },
  { question: "How to get the lines that appear exactly twice?", shortAnswer: "Use `uniq -c | grep '^ *2 ' | sed 's/^ *2 //'`.", explanation: "Count lines, filter for count 2, remove count prefix.", hint: "Or use `awk` after uniq.", level: "expert", codeExample: "sort file.txt | uniq -c | awk '$1==2 {print $0}'" },
  { question: "How to make uniq only compare a specific column (e.g., second column of CSV)?", shortAnswer: "Pre‑process with `cut` to extract that column, then uniq.", explanation: "Example: `cut -d',' -f2 file.csv | sort | uniq`.", hint: "If you need the full line, use `sort -k2` and then `uniq -f1` (if spaces).", level: "advanced", codeExample: "cut -d',' -f2 data.csv | sort | uniq" },
  { question: "Can uniq handle lines longer than 4KB?", shortAnswer: "Yes, uniq has no inherent line length limit (depends on system memory).", explanation: "Modern uniq can handle very long lines.", hint: "But very long lines may use more memory.", level: "expert", codeExample: "uniq huge.txt" },
  { question: "What is the exit status of uniq when no changes are made (i.e., no duplicates removed)?", shortAnswer: "0 (success) unless an error occurs.", explanation: "Uniq doesn't return 1 when input has no duplicates; only I/O errors cause non‑zero exit.", hint: "Check output, not exit code, for presence of duplicates.", level: "intermediate", codeExample: "uniq file.txt > /dev/null; echo $?" },
  { question: "How to use uniq on a string (not a file)?", shortAnswer: "Pipe echo: `echo -e 'a\\na\\nb' | sort | uniq`.", explanation: "Uniq reads stdin if no file provided.", hint: "Use `-e` with `echo` to interpret newlines.", level: "basic", codeExample: "echo -e 'apple\\nbanana\\napple' | sort | uniq" },
  { question: "What does `uniq --group` do (GNU uniq)?", shortAnswer: "Prints all duplicate groups with a delimiter (`:::`).", explanation: "Useful for showing groups of duplicate lines.", hint: "Not available on all systems.", level: "expert", codeExample: "uniq --group file.txt" },
  { question: "How to remove duplicate lines from a file in‑place (update the file)?", shortAnswer: "`sort -u file.txt -o file.txt`.", explanation: "`sort -o` can write back to the same file.", hint: "Or use `sort file.txt | uniq > tmp && mv tmp file.txt`.", level: "intermediate", codeExample: "sort -u file.txt -o file.txt" },
  { question: "Why might `uniq -f1` behave unexpectedly on lines starting with a space?", shortAnswer: "Leading spaces are considered part of field1? Actually whitespace separates fields; leading whitespace means the first field may be empty.", explanation: "Use `sed 's/^ *//'` to strip leading spaces before uniq.", hint: "Pre‑process to normalise.", level: "expert", codeExample: "sed 's/^ *//' file | uniq -f1" },
  { question: "How to get the count of unique words in a file using uniq?", shortAnswer: "`tr ' ' '\\n' < file | sort | uniq | wc -l`.", explanation: "Convert spaces to newlines to get one word per line, then dedupe and count.", hint: "Also split punctuation properly with `tr -s '[:punct:] '`.", level: "advanced", codeExample: "tr ' ' '\\n' < file.txt | sort | uniq | wc -l" },
  { question: "What is the difference between `uniq -d` and `uniq -D` (GNU)?", shortAnswer: "`-D` prints all duplicate lines (each occurrence), not just one per group.", explanation: "`-D` outputs every copy of duplicate lines; `-d` outputs only one copy.", hint: "Use `-D` to see all duplicates.", level: "advanced", codeExample: "uniq -D file.txt" },
  { question: "How to combine `uniq` with `grep` to find lines that appear exactly N times?", shortAnswer: "`sort file.txt | uniq -c | awk '$1==N {print $0}'`.", explanation: "Counts precede lines; awk filters by count.", hint: "Then `cut` or `sed` to remove the count prefix.", level: "expert", codeExample: "sort file.txt | uniq -c | awk '$1==3 {print substr($0, index($0,$2))}'" }
];

export default questions;