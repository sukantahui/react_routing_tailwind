const questions = [
  { question: "What does `cut -d',' -f2 file` do?", shortAnswer: "Extracts the second comma‑separated field from each line.", explanation: "`-d` sets delimiter to comma, `-f2` selects the second field.", hint: "Field numbering starts at 1.", level: "basic", codeExample: "echo 'a,b,c' | cut -d',' -f2   # b" },
  { question: "How to extract multiple non‑contiguous fields with cut?", shortAnswer: "Use comma‑separated list: `-f1,3,5`.", explanation: "You can also mix ranges: `-f1-3,5,7-9`.", hint: "No spaces after commas.", level: "basic", codeExample: "cut -d':' -f1,3,5 /etc/passwd" },
  { question: "What is the default field delimiter for cut?", shortAnswer: "Tab character.", explanation: "If you don't specify `-d`, cut expects tabs as field separators.", hint: "Use `-d$'\\t'` explicitly if needed.", level: "basic", codeExample: "cut -f2 tab_separated.txt" },
  { question: "How to change the output delimiter in cut?", shortAnswer: "Use `--output-delimiter='string'`.", explanation: "Works only in field mode (`-f`), not in character/byte mode.", hint: "Example: `--output-delimiter='|'`", level: "intermediate", codeExample: "cut -d',' -f1,3 --output-delimiter='|' data.csv" },
  { question: "What does `cut --complement -f2` do?", shortAnswer: "Prints all fields except field 2.", explanation: "`--complement` inverts the selection.", hint: "Useful for dropping columns.", level: "intermediate", codeExample: "echo 'a,b,c' | cut -d',' --complement -f2   # a,c" },
  { question: "How to extract characters 5 to 10 from each line?", shortAnswer: "`cut -c5-10 file`.", explanation: "Ranges are inclusive.", hint: "`-c5-` means from character 5 to end of line.", level: "basic", codeExample: "echo 'hello world' | cut -c7-11   # 'world'" },
  { question: "What is the difference between `-c` and `-b` in cut?", shortAnswer: "`-c` works on characters (respects multibyte), `-b` works on raw bytes.", explanation: "For ASCII, they are identical. For UTF‑8, `-b` might cut inside a multibyte char → corrupted output.", hint: "Use `-c` for human‑readable text.", level: "intermediate", codeExample: "echo 'αβγ' | cut -b1   # partial byte; use -c1 for 'α'" },
  { question: "Can cut reorder fields? For example, print field 3 then field 1?", shortAnswer: "GNU cut respects the order you specify: `cut -f3,1` prints field 3, then field 1.", explanation: "Other implementations may always print in original order. Test on your system.", hint: "For portability, use `awk '{print $3,$1}'`.", level: "advanced", codeExample: "echo 'a b c' | cut -d' ' -f3,1   # GNU: c a" },
  { question: "How to extract fields from a file with spaces as delimiter but multiple spaces?", shortAnswer: "`cut` treats each space as separate delimiter → empty fields. Use `tr -s ' '` first: `tr -s ' ' | cut -d' ' -f2`", explanation: "Better: use `awk '{print $2}'` which handles multiple spaces automatically.", hint: "`awk` is more robust.", level: "advanced", codeExample: "echo 'a   b   c' | tr -s ' ' | cut -d' ' -f2" },
  { question: "What happens if a line has fewer fields than requested?", shortAnswer: "cut prints nothing for that line (empty line).", explanation: "No error; missing fields result in blank output.", hint: "Filter first with `awk -F',' 'NF>=3' file` if needed.", level: "advanced", codeExample: "echo 'a,b' | cut -d',' -f3   # empty output" },
  { question: "How to use cut with `-d` for a delimiter that is a newline?", shortAnswer: "Not possible; cut works line‑by‑line and cannot change line delimiter.", explanation: "Use `tr` or `sed` to replace newlines first if needed.", hint: "For record separators other than newline, use `awk`.", level: "expert", codeExample: "# Not possible" },
  { question: "What is the purpose of `--output-delimiter` with character/byte mode?", shortAnswer: "It does nothing; `--output-delimiter` only works with field mode (`-f`).", explanation: "In character/byte mode, output is concatenated without separators.", hint: "No workaround; if you need separators, use `sed` or `awk`.", level: "advanced", codeExample: "cut -c1-3 --output-delimiter='|' file   # ignores output-delimiter" },
  { question: "How to print fields 1,2,3,5,7-9 with cut?", shortAnswer: "`cut -d',' -f1-3,5,7-9 file`.", explanation: "Ranges and single fields can be mixed with commas.", hint: "Ranges are inclusive.", level: "intermediate", codeExample: "cut -f1-3,5,7-9 data.tsv" },
  { question: "Why does cut sometimes output nothing even though fields exist?", shortAnswer: "You might have forgotten `-d` and cut is using default delimiter (tab).", explanation: "Check your delimiter and ensure it matches the file.", hint: "Use `cat -A file` to see tabs (displayed as `^I`).", level: "basic", codeExample: "cat -A file | cut -f2" },
  { question: "How to get the last field of each line using cut?", shortAnswer: "Cut doesn't support 'last field'. Use `rev | cut -d' ' -f1 | rev`.", explanation: "Reverse the line, cut first field (which was last), reverse back.", hint: "Or use `awk '{print $NF}'`.", level: "advanced", codeExample: "echo 'a b c' | rev | cut -d' ' -f1 | rev   # c" },
  { question: "What is the performance of cut compared to other tools?", shortAnswer: "cut is very fast for simple column extraction, often faster than awk.", explanation: "It is written in C and optimised for streaming, line‑by‑line processing.", hint: "Use cut when you don't need complex field processing.", level: "expert", codeExample: "time cut -d',' -f2 huge.csv" },
  { question: "How to cut by byte positions but handle multibyte gracefully?", shortAnswer: "You can't; use `-c` for character‑aware cutting. Or use `iconv -f UTF-8 -t ASCII//TRANSLIT` to convert first.", explanation: "Byte cutting on UTF‑8 may break characters.", hint: "Avoid `-b` unless you know the file is ASCII.", level: "expert", codeExample: "iconv -f UTF-8 -t ASCII//TRANSLIT file | cut -b1-10" },
  { question: "How to remove the first column (field) using cut?", shortAnswer: "`cut -f2- file` (if tab delimiter) or `cut -d',' -f2- file` for CSV.", explanation: "`-f2-` means fields 2 to end.", hint: "Or `cut --complement -f1`", level: "intermediate", codeExample: "echo 'a,b,c' | cut -d',' -f2-   # b,c" },
  { question: "What does `cut -c-10` mean?", shortAnswer: "Characters from start to position 10 (first 10 characters).", explanation: "`-c-10` is a range from 1 to 10 inclusive.", hint: "Same as `cut -c1-10`.", level: "basic", codeExample: "echo '1234567890abc' | cut -c-10   # 1234567890" },
  { question: "How to use cut with a delimiter that is a backslash?", shortAnswer: "Escape it: `cut -d'\\' -f2 file`.", explanation: "Backslash is special to shell and to cut; need proper quoting.", hint: "Use single quotes and double backslash.", level: "advanced", codeExample: "echo 'a\\b\\c' | cut -d'\\' -f2   # b" },
  { question: "Can cut handle multi‑character delimiters (e.g., '::')?", shortAnswer: "No, cut only supports single‑character delimiters.", explanation: "For multi‑character delimiters, use `awk` with `-F'::'` or `sed`.", hint: "`cut` is limited; time to learn `awk`.", level: "advanced", codeExample: "# Use awk: awk -F'::' '{print $2}' file" },
  { question: "What is the difference between `cut -f1,2` and `cut -f1,2 --output-delimiter=','` when input delimiter is tab?", shortAnswer: "Without `--output-delimiter`, output uses tab to separate fields. With `,`, it uses commas.", explanation: "You can change the output format without changing the input.", hint: "Useful for converting TSV to CSV.", level: "intermediate", codeExample: "cut -f1,2 --output-delimiter=',' file.tsv > file.csv" },
  { question: "How to extract a range of characters excluding a middle part (e.g., characters 1-5 and 10-15)?", shortAnswer: "`cut -c1-5,10-15 file`.", explanation: "Specify multiple ranges separated by commas.", hint: "Works for both `-c` and `-f`.", level: "basic", codeExample: "cut -c1-5,10-15 fixed_width.txt" },
  { question: "Why does `cut -d' ' -f2` fail on lines like 'a  b  c'?", shortAnswer: "Because consecutive spaces create empty fields. `-f2` prints empty if there are multiple spaces before the second token.", explanation: "`cut` treats each space as delimiter → fields: space, space, etc.", hint: "Use `tr -s ' '` or `awk '{print $2}'`.", level: "advanced", codeExample: "echo 'a  b  c' | cut -d' ' -f2   # empty output" },
  { question: "How to use cut with standard input (no file)?", shortAnswer: "Just pipe into cut: `echo 'a,b,c' | cut -d',' -f2`.", explanation: "If no file is given, cut reads from stdin.", hint: "Can also use `cut -d',' -f2 < file`.", level: "basic", codeExample: "cat file | cut -d' ' -f1" },
  { question: "What is the maximum field number cut can handle?", shortAnswer: "Limited by system memory and line length, but practically no fixed limit.", explanation: "cut reads lines one at a time, so field numbers can be very large.", hint: "However, huge field numbers will be slow.", level: "expert", codeExample: "# Works: cut -f1000000 huge_file (if line has that many fields)" },
  { question: "How to preserve the original line when a field is missing and still output something?", shortAnswer: "cut cannot; it outputs empty line. Use awk for conditional output: awk -F',' '{print ($3?$3:\"N/A\")}'", explanation: "awk gives you full control.", hint: "For complex logic, switch to awk.", level: "expert", codeExample: "echo 'a,b' | awk -F',' '{print ($3?$3:\"empty\")}'   # empty" },
  { question: "How to use `cut` with null byte as delimiter (e.g., output of `find -print0`)?", shortAnswer: "GNU cut supports -z (zero‑terminated lines) but not null delimiter for fields. Use awk -F'\\\\0'.", explanation: "Null‑delimited data is rare; cut isn't designed for it.", hint: "Use xargs -0 for such cases.", level: "expert", codeExample: "find . -print0 | cut -z -d'/' -f2   # may not work as expected" },
  { question: "What does `cut -f1 --complement` do?", shortAnswer: "Prints all fields except field 1.", explanation: "Equivalent to cut -f2- but clearer intent for removing column 1.", hint: "Use --complement for better readability when excluding columns.", level: "intermediate", codeExample: "echo 'a b c' | cut -d' ' -f1 --complement   # b c" },
  { question: "How to extract the 3rd character from each line using cut?", shortAnswer: "cut -c3 file.", explanation: "Single position, no range needed.", hint: "Remember counting starts at 1.", level: "basic", codeExample: "echo 'abcde' | cut -c3   # c" }
];

export default questions;