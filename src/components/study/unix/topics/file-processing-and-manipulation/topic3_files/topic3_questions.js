const questions = [
  { question: "What does `split -l 500 file.txt` do?", shortAnswer: "Splits file.txt into chunks of 500 lines each.", explanation: "Default is 1000 lines. Output files are named xaa, xab, etc.", hint: "Add a prefix: `split -l 500 file.txt chunk_`.", level: "basic", codeExample: "split -l 1000 big.log part_" },
  { question: "How to split a binary file into 1MB chunks?", shortAnswer: "`split -b 1M large.bin chunk_`", explanation: "Use suffixes: k, M, G for kilobytes, megabytes, gigabytes.", hint: "Ensure total chunks fit in disk space.", level: "basic", codeExample: "split -b 100k image.jpg img_" },
  { question: "What is the default chunk size for split when no option is given?", shortAnswer: "1000 lines.", explanation: "`split file` without `-l` or `-b` splits by 1000 lines.", hint: "Same as `split -l 1000 file`.", level: "basic", codeExample: "split data.txt   # creates xaa, xab, ..." },
  { question: "How to change the output file prefix?", shortAnswer: "Provide the prefix as the last argument: `split -l 500 input.txt myprefix_`.", explanation: "The prefix can be any string. Default is `x`.", hint: "Use meaningful prefixes to avoid confusion.", level: "basic", codeExample: "split -l 100 data.csv chunk_" },
  { question: "How to get numeric suffixes instead of alphabetic (aa, ab, ...)?", shortAnswer: "Use `-d` option: `split -d -l 500 file.txt part_`.", explanation: "Numeric suffixes start from 00. Use `-a N` to set length (e.g., `-a 3` gives 000,001).", hint: "Essential when more than 676 chunks (aa..zz).", level: "intermediate", codeExample: "split -d -a 4 -l 1000 huge.log log_" },
  { question: "How to split a file into a specific number of chunks (e.g., 5)?", shortAnswer: "Use `split -n l/5 file`.", explanation: "`-n` option splits by number of chunks. `l/5` means round‑robin lines into 5 files.", hint: "Also `-n 5` for byte‑level splitting (may break lines).", level: "advanced", codeExample: "split -n l/3 data.txt part_" },
  { question: "What is the difference between `-b` and `-C`?", shortAnswer: "`-b` splits strictly by bytes; `-C` tries to keep lines intact (line‑buffered).", explanation: "`-C` ensures no line is split, but chunk size is <= specified.", hint: "Use `-C` for text files when line integrity matters.", level: "advanced", codeExample: "split -C 1M large.log chunk_" },
  { question: "How to recombine split files back into the original?", shortAnswer: "`cat chunk_* > original`.", explanation: "Use `cat` in the correct order; default alphabetic order works if prefix consistent.", hint: "If numeric suffixes, `cat part_*` still works in numeric order (since 00,01,...).", level: "basic", codeExample: "cat part_* > reconstructed.csv" },
  { question: "What happens if you run split on a file without enough disk space?", shortAnswer: "split writes partial chunks until disk full, then fails with 'write error'.", explanation: "Check available space before splitting large files.", hint: "Use `df -h .` to see free space.", level: "intermediate", codeExample: "# Will fail if no space" },
  { question: "How to split a file but keep the header row in each chunk?", shortAnswer: "Use `tail -n +2` to remove header, split, then add header back per chunk.", explanation: "Example: `tail -n +2 data.csv | split -l 100 - chunk_ && for f in chunk_*; do (echo 'header' | cat - $f > $f.tmp && mv $f.tmp $f); done`", hint: "Or use `--filter` for one‑step processing.", level: "expert", codeExample: "tail -n +2 data.csv | split -l 500 --filter='{ echo \"Header\"; cat; } > $FILE'" },
  { question: "What does `split -d -a 3 -l 500 file part_` produce?", shortAnswer: "Files named part_000, part_001, part_002, ... up to part_999 (since 3 digits).", explanation: "`-d` numeric, `-a 3` suffix length 3, prefix `part_`.", hint: "Allows up to 1000 chunks.", level: "intermediate", codeExample: "split -d -a 4 -l 1000 data.txt chunk_" },
  { question: "Can split read from standard input? How?", shortAnswer: "Yes, use `-` as input file: `cat huge.log | split -l 1000 - chunk_`.", explanation: "The dash represents stdin.", hint: "Useful when piping from other commands.", level: "intermediate", codeExample: "grep 'ERROR' app.log | split -l 500 - error_chunk_" },
  { question: "How to split a file into chunks of a specific number of bytes but not split lines?", shortAnswer: "Use `split -C` (line‑buffered).", explanation: "`split -C 1M` produces chunks of up to 1MB, but lines are never split across chunks.", hint: "Slower than `-b` but safer for text.", level: "advanced", codeExample: "split -C 1M huge.sql chunk_" },
  { question: "What is the maximum number of chunks possible with default alphabetic suffixes?", shortAnswer: "676 (aa to zz).", explanation: "Two‑letter suffix from aa to zz. Use `-a` to increase length.", hint: "For more than 676, use `-d` or increase `-a`.", level: "expert", codeExample: "# 2 letters: 26*26=676" },
  { question: "How to split a file into N chunks regardless of size (balanced)?", shortAnswer: "`split -n l/N file` (line‑based round‑robin) or `split -n N file` (byte‑based).", explanation: "`-n l/N` distributes lines cyclically across N files; `-n N` splits by bytes (but may break lines).", hint: "Not perfect balance but good enough for many purposes.", level: "advanced", codeExample: "split -n l/4 balanced.txt part_" },
  { question: "What does the `--verbose` option do?", shortAnswer: "Prints a message for each output file created.", explanation: "Useful for monitoring when splitting huge files.", hint: "Combine with `-d` and `-a`.", level: "basic", codeExample: "split --verbose -l 1000 big.log chunk_" },
  { question: "How to split a file but ignore empty lines for chunk size?", shortAnswer: "Not directly; pre‑filter with `grep -v '^$'` before piping.", explanation: "Example: `grep -v '^$' file | split -l 100 - chunk_`", hint: "Empty lines are still counted as lines.", level: "advanced", codeExample: "sed '/^$/d' file | split -l 100 - chunk_" },
  { question: "Why does split sometimes produce fewer chunks than expected?", shortAnswer: "Because the last chunk may be smaller than the specified size.", explanation: "If total lines not multiple of chunk size, last chunk is remainder.", hint: "This is normal; check total lines with `wc -l`.", level: "basic", codeExample: "# If 150 lines, split -l 100 gives two chunks: 100 and 50 lines." },
  { question: "How to use split with `--filter` to gzip each chunk on the fly?", shortAnswer: "`split -l 1000 --filter='gzip > $FILE.gz' big.log`", explanation: "`$FILE` is the name of the output chunk; `gzip` compresses it.", hint: "Saves disk space and time.", level: "expert", codeExample: "split -b 10M --filter='xz > $FILE.xz' binary.bin" },
  { question: "Can split create chunks that have a specific extension (e.g., .txt)?", shortAnswer: "No, but you can rename after split or use `--filter` to add extension.", explanation: "Example: `split -l 1000 --filter='cat > $FILE.txt' file`.", hint: "The filter can do any processing.", level: "advanced", codeExample: "split -l 500 --filter='cat > $FILE.log' application.log" },
  { question: "What is the performance impact of splitting a very large file (e.g., 100GB)?", shortAnswer: "Split reads sequentially and writes multiple files; I/O bound. Use `-d` numeric suffixes for faster naming.", explanation: "On SSDs, splitting 100GB takes minutes; on HDDs longer.", hint: "For parallel processing, consider splitting without writing intermediate files (e.g., `split --filter='process'`).", level: "expert", codeExample: "time split -l 1000000 huge.csv part_" },
  { question: "How to split a file by line count but ensure each chunk starts with a header?", shortAnswer: "Use `split -l` with `--filter` to add header: `split -l 500 --filter='{ echo \"Header\"; cat; } > $FILE' data.csv`", explanation: "First chunk gets header; subsequent ones also get header, which may duplicate.", hint: "Better to split after removing header, then re‑add header to first chunk only.", level: "expert", codeExample: "tail -n +2 data.csv | split -l 500 --filter='{ [[ \"$FILE\" == \"xaa\" ]] && echo \"Header\"; cat; } > $FILE'" },
  { question: "What is the difference between `split -n N` and `split -l`?", shortAnswer: "`-n N` splits into exactly N chunks based on bytes (possible mid‑line). `-l` splits into variable number of line‑based chunks.", explanation: "`-n l/N` (line round‑robin) splits into fixed‑count line chunks, but lines are distributed cyclically, not sequentially.", hint: "For sequential line chunks, use `-l`.", level: "expert", codeExample: "# Sequential: split -l 1000 file\n# Round‑robin: split -n l/10 file" },
  { question: "How to split a file but preserve the original file's mode/permissions?", shortAnswer: "Use `umask` or `chmod` after split; split creates files with default umask (666 minus umask).", explanation: "Split does not copy permissions from original.", hint: "After splitting, run `chmod --reference=original chunk_*`.", level: "advanced", codeExample: "chmod 644 chunk_*  # example" },
  { question: "What is the purpose of `-a` option?", shortAnswer: "Sets the length of the suffix (number of letters/digits used).", explanation: "Default is 2 (`aa`, `ab`). For more than 676 chunks, increase suffix length.", hint: "Use with `-d` for numeric suffixes.", level: "intermediate", codeExample: "split -a 4 -l 1000 long.txt part_" },
  { question: "How to split a file into chunks of exactly 1000 bytes without any line break?", shortAnswer: "Not possible with standard split; use `dd` or write a custom script.", explanation: "`split -b 1000` may break lines; but if you don't mind, it works.", hint: "For binary data, `-b` is fine.", level: "expert", codeExample: "# split -b 1000 file - will split at byte boundaries, possibly mid‑line." },
  { question: "Does split support splitting by number of records based on a regex delimiter?", shortAnswer: "No, split works only on line or byte boundaries. Use `csplit` for regex‑based splitting.", explanation: "`csplit` splits based on context (patterns).", hint: "`csplit` is more advanced but less common.", level: "expert", codeExample: "csplit file '/^Chapter/' '{*}'" },
  { question: "How to split a file and then process each chunk in parallel with GNU Parallel?", shortAnswer: "`split -l 1000 file chunk_ && parallel 'process {}' ::: chunk_*`", explanation: "Split creates chunks, then GNU Parallel runs a command on each.", hint: "Combine `--filter` to avoid writing to disk if possible.", level: "advanced", codeExample: "split -l 10000 big.log part_ && parallel --bar 'gzip {}' ::: part_*" },
  { question: "What is the behaviour of split when the input file is empty?", shortAnswer: "Creates no output files (since there are no lines/bytes).", explanation: "Exit status 0, no files created.", hint: "Use `-l 1` with empty file: no chunks.", level: "basic", codeExample: "touch empty; split -l 10 empty part_   # no files" },
  { question: "How to split a file but include a moving window (overlapping chunks)?", shortAnswer: "Not possible with split; use `awk` or `sed` scripts with sliding windows.", explanation: "Split creates disjoint chunks.", hint: "For overlapping, consider using `tail`/`head` combinations in a loop.", level: "expert", codeExample: "# Not a split task." }
];

export default questions;