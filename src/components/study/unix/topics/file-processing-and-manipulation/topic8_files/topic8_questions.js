const questions = [
  { question: "What does `cmp file1 file2` output by default?", shortAnswer: "The byte number and line number of the first difference.", explanation: "Output format: `file1 file2 differ: byte XXX, line YY`.", hint: "No output if files are identical.", level: "basic", codeExample: "cmp a.txt b.txt" },
  { question: "What is the exit status of cmp when files are identical?", shortAnswer: "0", explanation: "Exit 0: same; 1: differ; >1: error.", hint: "Useful in shell scripts.", level: "basic", codeExample: "if cmp -s file1 file2; then echo 'same'; fi" },
  { question: "How to suppress output and only get exit status?", shortAnswer: "`cmp -s file1 file2`.", explanation: "`-s` (silent) suppresses messages, only return code.", hint: "Use in scripts.", level: "basic", codeExample: "cmp -s file1 file2 || echo 'files differ'" },
  { question: "How to see the actual differing byte values in octal?", shortAnswer: "`cmp -b file1 file2`.", explanation: "`-b` prints the byte values (octal) and ASCII representation.", hint: "Useful for binary data.", level: "intermediate", codeExample: "cmp -b binary1 binary2" },
  { question: "How to list all differing bytes (not just first)?", shortAnswer: "`cmp -l file1 file2`.", explanation: "`-l` (verbose) prints byte number and differing byte values for each difference.", hint: "For many differences, output can be large.", level: "advanced", codeExample: "cmp -l file1 file2 | head -10" },
  { question: "How to limit comparison to first N bytes?", shortAnswer: "`cmp -n N file1 file2`.", explanation: "`-n` stops after comparing N bytes from each file.", hint: "Fast check of headers.", level: "intermediate", codeExample: "cmp -n 1000 boot1.img boot2.img" },
  { question: "How to skip bytes at the beginning of files?", shortAnswer: "`cmp file1 file2 SKIP1 SKIP2`.", explanation: "SKIP1 and SKIP2 are offsets (in bytes) to start comparison.", hint: "Useful for skipping file headers.", level: "advanced", codeExample: "cmp file1 file2 512 512   # skip first 512 bytes" },
  { question: "Can cmp compare binary files? If so, how?", shortAnswer: "Yes, cmp works on any file type (binary or text). No special flag needed.", explanation: "Cmp compares byte by byte, not line oriented.", hint: "Better than diff for binaries.", level: "basic", codeExample: "cmp image1.jpg image2.jpg" },
  { question: "What is the difference between cmp and diff for text files?", shortAnswer: "cmp finds first byte difference; diff shows line‑based changes (adds, removes, context).", explanation: "Cmp is precise and fast, diff is readable.", hint: "Use cmp for exact binary equality, diff for understanding changes.", level: "intermediate", codeExample: "# cmp: byte offset; diff: line changes" },
  { question: "Why does cmp sometimes report a difference at EOF (end of file)?", shortAnswer: "When one file is longer than the other, cmp sees the extra bytes as differences.", explanation: "It reports the first extra byte (EOF of shorter file).", hint: "Use `-n` to compare only up to shorter length.", level: "advanced", codeExample: "cmp short.txt long.txt   # shows diff at byte length+1" },
  { question: "How to compare two files but ignore trailing newline differences?", shortAnswer: "Cmp cannot ignore newlines, but you can pre‑process: `cmp <(printf '%s' \"$(cat file1)\") <(printf '%s' \"$(cat file2)\")`.", explanation: "Removes trailing newlines before comparison.", hint: "Simpler: use diff -b or -w.", level: "expert", codeExample: "# Complex; use diff for text" },
  { question: "What does `cmp -i OFFSET` do? (Note: some versions support `--ignore-initial`)", shortAnswer: "Skips the first OFFSET bytes of both files (like positional skip).", explanation: "GNU cmp supports `-i OFFSET` (skip OFFSET bytes).", hint: "Alternative to positional arguments.", level: "advanced", codeExample: "cmp -i 100 file1 file2" },
  { question: "How to get the byte offset of the first difference without extra text?", shortAnswer: "Use `cmp -l file1 file2 | head -1 | awk '{print $1}'`.", explanation: "Parse `-l` output to extract first byte number.", hint: "Or `cmp -n 1` to force early diff? Not trivial.", level: "expert", codeExample: "cmp -l a b 2>/dev/null | head -1 | cut -d' ' -f1" },
  { question: "What is the maximum file size cmp can handle?", shortAnswer: "No intrinsic limit; it streams bytes, works with huge files.", explanation: "Memory usage is minimal; can compare terabyte files.", hint: "But linear scan may be slow.", level: "expert", codeExample: "cmp huge1.dat huge2.dat   # works" },
  { question: "How to compare two files and show differing bytes in hex instead of octal?", shortAnswer: "Cmp only outputs octal by default. Use `cmp -l file1 file2 | awk '{printf \"%8d 0x%02x 0x%02x\\n\", $1, $2, $3}'`.", explanation: "Awk converts decimal to hex.", hint: "Or use `xxd` after locating differences.", level: "expert", codeExample: "# Custom script" },
  { question: "How to use cmp in a find command to detect changed files?", shortAnswer: "`find . -name '*.bak' -exec cmp -s {} {}.orig \\; -print`.", explanation: "Prints files that differ from their original.", hint: "Combine with `-execdir`.", level: "advanced", codeExample: "find . -name '*.txt' -exec cmp -s {} {}.old \\; -o -print" },
  { question: "Why does cmp sometimes output 'cmp: EOF on file1'?", shortAnswer: "One file is a prefix of the other; cmp reaches end of shorter file before the other.", explanation: "Indicates that the longer file has extra data after the shorter ends.", hint: "`-n` can limit comparison to the shorter length.", level: "intermediate", codeExample: "# Example: short file is complete prefix" },
  { question: "How to compare files using cmp but ignore a certain range of bytes (e.g., timestamps at a fixed offset)?", shortAnswer: "Use skip offsets: `cmp file1 file2 0 0` to start at beginning, but cannot skip a middle range. Use `dd` to extract segments.", explanation: "Complex; better to pre‑process with `dd` or `truncate`.", hint: "Not a direct cmp feature.", level: "expert", codeExample: "dd if=file1 bs=1 skip=10 count=100 2>/dev/null | cmp - dd if=file2 bs=1 skip=10 count=100 2>/dev/null" },
  { question: "Can cmp compare more than two files at once?", shortAnswer: "No, only two files. Use loops or `md5sum` for multiple files.", explanation: "Compare pairwise.", hint: "For many files, checksums are more efficient.", level: "basic", codeExample: "for f in *.bin; do cmp ref.bin $f; done" },
  { question: "How to make cmp treat files as text and output line numbers correctly when there are binary bytes?", shortAnswer: "Cmp always counts newline bytes to determine line number. Binary bytes that are not newline still advance line number only when newline appears.", explanation: "Line number is based on newline count, not text lines.", hint: "For binary, ignore line number.", level: "intermediate", codeExample: "cmp binary.bin text.txt" },
  { question: "What is the difference between `cmp -s` and redirecting output to `/dev/null`?", shortAnswer: "`cmp -s` also suppresses error messages (like EOF) and sets exit code correctly. Redirecting stdout may still print errors to stderr.", explanation: "`-s` is cleaner.", hint: "Always use `-s` in scripts.", level: "basic", codeExample: "cmp -s a b || echo 'differ'" },
  { question: "How to compare two files and output the first differing character (as a symbol)?", shortAnswer: "`cmp -b file1 file2 | awk '{print $5}'` may give the character; but better to use hexdump.", explanation: "Parsing cmp -b output is unreliable.", hint: "Use `cmp -l` and `xxd`.", level: "expert", codeExample: "pos=$(cmp -l a b | head -1 | cut -f1); xxd -s $((pos-1)) -l 1 a" },
  { question: "Why does cmp report 'cmp: file1: No such file or directory'?", shortAnswer: "File does not exist; check path.", explanation: "Not a cmp feature; shell error.", hint: "Ensure files exist before comparing.", level: "basic", codeExample: "cmp missing.txt existing.txt" },
  { question: "How to compare two files and return the number of differing bytes (not just first)?", shortAnswer: "`cmp -l file1 file2 | wc -l`.", explanation: "Counts lines equals number of differing bytes.", hint: "Useful for quantifying similarity.", level: "advanced", codeExample: "diff_cnt=$(cmp -l a b 2>/dev/null | wc -l)" },
  { question: "What does `cmp --help` show?", shortAnswer: "Shows usage summary and available options.", explanation: "Standard for GNU cmp.", hint: "Also `man cmp`.", level: "basic", codeExample: "cmp --help" },
  { question: "Can cmp be used to compare partitions or block devices? e.g., `/dev/sda1` vs `/dev/sdb1`", shortAnswer: "Yes, but be cautious; cmp reads raw device data.", explanation: "Example: `cmp -n 1M /dev/sda1 /dev/sdb1` compares first 1MB.", hint: "Requires root access for devices.", level: "expert", codeExample: "sudo cmp -n 1048576 /dev/sda1 /dev/sdb1" },
  { question: "What is the purpose of `--ignore-initial=SKIP1:SKIP2`?", shortAnswer: "Skips SKIP1 bytes from first file, SKIP2 from second file before comparing.", explanation: "GNU extension, similar to positional skip arguments.", hint: "More flexible than plain skip numbers.", level: "advanced", codeExample: "cmp --ignore-initial=100:200 file1 file2" },
  { question: "How to compare two directories with cmp (recursively)?", shortAnswer: "Cmp cannot directly compare directories; use `find` + `cmp` or `diff -r`.", explanation: "Example: `find dir1 -type f -exec cmp {} dir2/\\{\\} \\;`", hint: "Better to use `diff -r`.", level: "advanced", codeExample: "# Complex" },
  { question: "Why does `cmp -l` output byte numbers in decimal but values in octal?", shortAnswer: "Historical reasons; octal for byte values is common in Unix.", explanation: "Decimal offset, octal byte values.", hint: "Use `awk` to convert to hex if needed.", level: "intermediate", codeExample: "cmp -l a b | awk '{printf \"%d 0x%02x\\n\", $1, $2}'" },
  { question: "How to use cmp to verify a file against a known signature (first few bytes)?", shortAnswer: "`cmp -n 4 file.bin signature.bin`.", explanation: "Check only the first 4 bytes (magic number).", hint: "Common for file type detection.", level: "intermediate", codeExample: "cmp -n 4 file.pdf '%PDF'" }
];

export default questions;