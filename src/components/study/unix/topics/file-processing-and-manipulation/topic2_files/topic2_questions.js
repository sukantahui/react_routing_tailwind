const questions = [
  { question: "What does `paste file1 file2` do?", shortAnswer: "Merges corresponding lines from file1 and file2 side‑by‑side with tab delimiter.", explanation: "Line 1 of file1 is placed, then tab, then line 1 of file2." hint: "Opposite of `cut`.", level: "basic", codeExample: "paste names.txt scores.txt" },
  { question: "How to change the delimiter when pasting?", shortAnswer: "Use `-d` option: `paste -d',' file1 file2`.", explanation: "The delimiter can be any character, or multiple characters (cyclically).", hint: "`-d '|'` for pipe‑separated.", level: "basic", codeExample: "paste -d':' file1 file2" },
  { question: "What does `paste -s file` do?", shortAnswer: "Serial mode: pastes all lines of the single file into one row (across columns).", explanation: "Converts a vertical list into a horizontal series.", hint: "Useful for transposing data.", level: "intermediate", codeExample: "paste -s -d',' data.txt   # one comma‑separated row" },
  { question: "How to paste three or more files together?", shortAnswer: "Simply list them: `paste file1 file2 file3`.", explanation: "Each file contributes one column; columns separated by delimiter.", hint: "All files are read in parallel line by line.", level: "basic", codeExample: "paste col1.txt col2.txt col3.txt" },
  { question: "What happens if files have different line counts?", shortAnswer: "paste outputs lines until the longest file ends, leaving empty fields for shorter files.", explanation: "Missing fields are empty (no delimiter between them?). Actually delimiter appears but nothing between.", hint: "Be careful; you may need to pad shorter files.", level: "intermediate", codeExample: "echo -e '1\n2' | paste - <(echo 'a')   # Output: '1 a', then '2 '" },
  { question: "How to use standard input with paste?", shortAnswer: "Use `-` as filename: `command | paste - file2`.", explanation: "The `-` represents stdin, which is read as one of the columns.", hint: "Example: `cut -f1 file | paste - file2`.", level: "intermediate", codeExample: "echo 'hello' | paste - /tmp/other.txt" },
  { question: "Can paste take output from two commands directly? (process substitution)", shortAnswer: "Yes, in bash: `paste <(cmd1) <(cmd2)`.", explanation: "Process substitution creates temporary file descriptors that behave like files.", hint: "Useful for combining outputs without writing temp files.", level: "advanced", codeExample: "paste <(ls -1) <(ls -l | awk '{print $5}')" },
  { question: "What is the default delimiter for paste?", shortAnswer: "Tab character (\\t).", explanation: "Unless changed with `-d`, paste uses tab to separate columns.", hint: "Use `cat -A` to see tabs as `^I`.", level: "basic", codeExample: "paste file1 file2 | cat -A" },
  { question: "How to produce comma‑separated output (CSV) from paste?", shortAnswer: "Use `-d ','`.", explanation: "Example: `paste -d ',' names.txt scores.txt > output.csv`.", hint: "Combine with header row echoed first.", level: "basic", codeExample: "echo 'Name,Score' | cat - <(paste -d',' names.txt scores.txt)" },
  { question: "What does `paste -d ':,' file1 file2 file3` do?", shortAnswer: "Uses delimiters cyclically: `:` between col1 & col2, `,` between col2 & col3, then repeats for more files.", explanation: "If more than 2 files, the list of delimiters cycles.", hint: "Uncommon but powerful for complex formatting.", level: "expert", codeExample: "paste -d '|;' a b c   # a|b;c" },
  { question: "How to join two files but only the first N lines?", shortAnswer: "Use `head` on each file before pasting: `paste <(head -10 file1) <(head -10 file2)`.", explanation: "Or use `head` combined with process substitution.", hint: "Even works with different numbers of lines.", level: "advanced", codeExample: "paste <(head -5 file1) <(head -3 file2)" },
  { question: "What is the difference between `paste file1 file2` and `pr -T -s' ' file1 file2`?", shortAnswer: "Both merge files horizontally, but `pr` has more formatting options.", explanation: "`paste` is simpler and faster for plain merging.", hint: "Stick with `paste` for basic column Joins.", level: "advanced", codeExample: "pr -T -s' ' file1 file2" },
  { question: "How to use paste to add a header row to a dataset?", shortAnswer: "`echo -e 'Col1\\tCol2' | paste - data.txt`", explanation: "The `-` reads the header from stdin, then paste merges it as the first row.", hint: "Works with custom delimiters too.", level: "intermediate", codeExample: "echo 'Name,Score' | paste -d',' - data.csv" },
  { question: "Does paste modify the original files?", shortAnswer: "No, paste only reads files and writes to stdout, leaving originals unchanged.", explanation: "Redirect output to a new file to save.", hint: "Always safe to use without backup.", level: "basic", codeExample: "paste a b > combined.txt" },
  { question: "How to paste files in vertical direction (i.e., concatenate)?", shortAnswer: "`cat file1 file2` for vertical concatenation; `paste` is for horizontal.", explanation: "`paste` is the opposite of `cat` – it merges across, not down.", hint: "If you want rows, just use `cat`.", level: "basic", codeExample: "cat file1 file2 > vertical.txt" },
  { question: "How to create a table of contents from two lists using paste?", shortAnswer: "`paste -d '.' chapters.txt pages.txt > toc.txt`.", explanation: "Creates lines like `Chapter1.5` if chapters.txt has `Chapter1` and pages.txt has `5`.", hint: "Delimiter can be anything.", level: "intermediate", codeExample: "paste -d ' - ' titles.txt authors.txt" },
  { question: "What happens if you paste a single file without `-s`?", shortAnswer: "It outputs the file unchanged (one column).", explanation: "Pasting one file is equivalent to `cat` but each line becomes a single column.", hint: "Useful for aligning with `column -t`.", level: "basic", codeExample: "paste single.txt   # same as cat single.txt" },
  { question: "How to paste two files but swap the column order?", shortAnswer: "`paste file2 file1`.", explanation: "Order of files determines order of columns.", hint: "Can't reorder within a file; use `cut` before pasting.", level: "basic", codeExample: "paste scores.txt names.txt" },
  { question: "Why does `paste -d' ' file1 file2` produce double spaces sometimes?", shortAnswer: "Because the input lines may contain trailing spaces or the delimiter is added to existing spaces.", explanation: "Use `paste -d ' '` but if data has spaces, it's ambiguous.", hint: "Consider using a unique delimiter then translate.", level: "advanced", codeExample: "paste -d'|' file1 file2 | tr '|' ' '" },
  { question: "How to use paste to combine every two lines of a single file into one line?", shortAnswer: "`paste -d' ' - - < file` (two dashes).", explanation: "Each dash represents one column; two dashes read two lines per row.", hint: "Number of dashes = number of fields per row.", level: "expert", codeExample: "paste -d',' - - < file.txt   # two lines merged with comma" },
  { question: "What is the performance of paste on very large files?", shortAnswer: "Very efficient; reads files sequentially in parallel, line by line, using minimal memory.", explanation: "It doesn't load entire files, only current line(s).", hint: "Suitable for multi‑gigabyte files.", level: "expert", codeExample: "time paste huge1.txt huge2.txt > /dev/null" },
  { question: "How to pad shorter files with a default value instead of empty fields?", shortAnswer: "`paste` cannot pad; pre‑process with `awk` or `sed` to add missing lines.", explanation: "Example: `awk '{print $0} END {for(i=NR;i<max;i++) print \"\"}'`", hint: "Or use `pr` with `-e` and `-s`.", level: "expert", codeExample: "# Complex; consider using `join` for relational data." },
  { question: "What does `paste - - - < file` do with 3 dashes?", shortAnswer: "Reads three lines of file at a time and pastes them side‑by‑side in one row (three columns).", explanation: "Each dash is a placeholder for one column reading from stdin.", hint: "Useful for converting vertical to wide format.", level: "expert", codeExample: "seq 9 | paste - - -   # three rows, three columns" },
  { question: "How to combine `paste` with `column -t` for aligned output?", shortAnswer: "`paste file1 file2 | column -t`.", explanation: "`column -t` formats the tab‑separated output into nicely aligned columns.", hint: "Human‑readable tables without extra tools.", level: "intermediate", codeExample: "paste -d',' data1 data2 | column -t -s','" },
  { question: "Why does `paste -d',' file1 file2` sometimes create trailing commas?", shortAnswer: "Because one file may have fewer lines, leaving an empty field after the delimiter.", explanation: "Trailing delimiter appears only if the last field is missing.", hint: "Post‑process with `sed 's/,$//'`.", level: "advanced", codeExample: "paste -d',' a b | sed 's/,$//'" },
  { question: "How to use paste with process substitution in a POSIX‑compliant way?", shortAnswer: "POSIX doesn't have process substitution; use temporary files or named pipes.", explanation: "Bash and Zsh support it, but dash does not.", hint: "For portability, write to temp files first.", level: "expert", codeExample: "cmd1 > tmp1; cmd2 > tmp2; paste tmp1 tmp2; rm tmp1 tmp2" },
  { question: "How to paste files where the delimiter is a newline? (Impossible)", shortAnswer: "Not possible; paste works line‑by‑line; newline is the record separator.", explanation: "Use `tr` to change newline to something else first.", hint: "Or use `awk` with `ORS`.", level: "expert", codeExample: "# Not directly possible" },
  { question: "What does `paste -s -d '\\n' file` do?", shortAnswer: "Since newline is default, it's same as `paste -s` (puts lines in a row, but since delimiter is newline, it's just a single column? Actually `-s` with newline delimiter puts each original line on a new line; that is, no change. Wait: `-d '\\n'` is the default record separator, so `paste -s` without `-d` does nothing different from `cat`? Actually `-s` alone uses tab delimiter. With `-d '\\n'`, it separates fields by newlines, which would put each field on its own line – effectively original file unchanged. So it's a no‑op.", explanation: "It's confusing; better to avoid.", hint: "Stick to usual delimiters.", level: "expert", codeExample: "paste -s -d '|' file   # pipe‑separated row" },
  { question: "How to use paste to create a matrix from a list of numbers?", shortAnswer: "Use `paste - - - ...` with appropriate number of dashes.", explanation: "Example: `seq 9 | paste - - -` creates a 3x3 matrix.", hint: "Number of dashes = columns.", level: "advanced", codeExample: "seq 12 | paste - - - - | column -t" }
];

export default questions;