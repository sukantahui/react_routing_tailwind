const questions = [
  { question: "What does `comm file1 file2` output by default?", shortAnswer: "Three columns: lines only in file1, lines only in file2, and lines common to both.", explanation: "Columns separated by tabs. Column 1: only file1; Column 2: only file2; Column 3: both files.", hint: "Files must be sorted.", level: "basic", codeExample: "comm file1.txt file2.txt" },
  { question: "What happens if input files are not sorted?", shortAnswer: "comm output is undefined/wrong; it may misclassify lines.", explanation: "comm relies on sorted order to compare line by line.", hint: "Always sort before using comm.", level: "basic", codeExample: "# Run sort first: comm <(sort f1) <(sort f2)" },
  { question: "How to suppress the first column (lines only in first file)?", shortAnswer: "`comm -1 file1 file2`.", explanation: "`-1` hides column 1.", hint: "Combine: `comm -23` = lines only in file1.", level: "basic", codeExample: "comm -1 file1 file2" },
  { question: "How to get only lines common to both files (intersection)?", shortAnswer: "`comm -12 file1 file2`.", explanation: "Suppress columns 1 and 2, leaving only column 3.", hint: "Equivalent to `grep -Fxf` but faster on sorted files.", level: "intermediate", codeExample: "comm -12 sorted1 sorted2" },
  { question: "How to get lines that are only in the first file (set difference)?", shortAnswer: "`comm -23 file1 file2`.", explanation: "Suppress columns 2 and 3, leaving column 1.", hint: "Similar to `grep -vxFf file2 file1` but requires sorting.", level: "intermediate", codeExample: "comm -23 file1 file2" },
  { question: "How to get lines that are only in the second file?", shortAnswer: "`comm -13 file1 file2`.", explanation: "Suppress columns 1 and 3, leaving column 2.", hint: "Output: lines in file2 not in file1.", level: "intermediate", codeExample: "comm -13 file1 file2" },
  { question: "Can `comm` handle duplicate lines in input files?", shortAnswer: "Yes, but output may include duplicates; often better to `sort -u` first.", explanation: "comm processes each line sequentially, duplicates will appear multiple times in output.", hint: "Use `uniq` before comm if you want set semantics.", level: "advanced", codeExample: "comm <(sort -u f1) <(sort -u f2)" },
  { question: "How to make `comm` case‑insensitive?", shortAnswer: "Pre‑process files with `tr '[:upper:]' '[:lower:]'` or `sed 's/.*/\\L&/'` (GNU sed).", explanation: "Comm is case‑sensitive by default; convert both files to same case.", hint: "Example: `comm <(tr 'A-Z' 'a-z' < file1 | sort) <(tr 'A-Z' 'a-z' < file2 | sort)`", level: "advanced", codeExample: "# Use process substitution with tr" },
  { question: "What does `comm -23 <(sort f1) <(sort f2)` do?", shortAnswer: "Shows lines present in f1 but not in f2 (difference), after sorting.", explanation: "Process substitution sorts on the fly without modifying original files.", hint: "Very common pattern.", level: "advanced", codeExample: "comm -23 <(sort file1) <(sort file2)" },
  { question: "How to get the union of two files (all unique lines) using comm?", shortAnswer: "`comm -3 file1 file2 | sed 's/^\\t//'` then merge columns; simpler: `sort -u file1 file2`.", explanation: "comm -3 suppresses common lines, but outputs column1 and column2; removing the leading tabs can be messy.", hint: "Use `sort -u` for union.", level: "advanced", codeExample: "sort -u file1 file2   # union" },
  { question: "What is the exit status of `comm` when files are identical?", shortAnswer: "0 (success).", explanation: "comm exits 0 unless error; it doesn't use exit status to indicate differences (unlike diff).", hint: "Check output, not exit code.", level: "basic", codeExample: "if comm -12 file1 file2 >/dev/null; then echo 'common lines exist'; fi" },
  { question: "How to ignore leading spaces when using comm?", shortAnswer: "Pre‑process with `sed 's/^[[:space:]]*//'` before sorting.", explanation: "Whitespace affects sorting; strip it for comparison.", hint: "Use `sort -b` to ignore leading blanks.", level: "advanced", codeExample: "comm <(sed 's/^ *//' file1 | sort) <(sed 's/^ *//' file2 | sort)" },
  { question: "Can `comm` compare files with different delimiters (e.g., CSV)?", shortAnswer: "Indirectly: extract the column to compare, then comm on the extracted lists.", explanation: "Comm works on entire lines; use cut/awk to get the key field, then sort and comm.", hint: "Example: `cut -d',' -f1 file1 | sort`", level: "intermediate", codeExample: "comm <(cut -d',' -f1 file1 | sort) <(cut -d',' -f1 file2 | sort)" },
  { question: "How to find lines that are in file1 but not in file2 using `comm` but ignoring a specific column?", shortAnswer: "Use `awk` or `cut` to extract the relevant part of each line, then comm.", explanation: "Comm compares entire lines; reduce each line to the field you care about.", hint: "Create temporary files with the simplified lines.", level: "expert", codeExample: "awk '{print $2}' file1 | sort > key1; awk '{print $2}' file2 | sort > key2; comm -23 key1 key2" },
  { question: "What is the difference between `comm -23` and `diff` for showing lines only in first file?", shortAnswer: "`comm -23` shows only the lines themselves; `diff` shows context and line numbers.", explanation: "Diff is for understanding changes, comm is for set operations.", hint: "Use comm when you just need the list of lines.", level: "intermediate", codeExample: "# comm: lines only in file1; diff: '< line' format" },
  { question: "How to make `comm` treat tabs as delimiters for columns in output?", shortAnswer: "It already does; each column is separated by a tab. You can't change the delimiter.", explanation: "comm's output delimiter is fixed to tab; use `tr` or `sed` to convert to other delimiters.", hint: "Example: `comm file1 file2 | tr '\\t' ','` for CSV.", level: "advanced", codeExample: "comm file1 file2 | sed 's/\\t/|/g'" },
  { question: "Why does `comm` sometimes output a blank line at the top?", shortAnswer: "If one file has a leading empty line (or starts with tab?), but not typical; usually due to empty lines in input.", explanation: "Empty lines are compared like any other line; they may appear in output.", hint: "Use `grep -v '^$'` to remove empty lines before processing.", level: "intermediate", codeExample: "comm <(grep -v '^$' file1 | sort) <(grep -v '^$' file2 | sort)" },
  { question: "How to compare two large sorted files with `comm` efficiently?", shortAnswer: "comm is already efficient; it streams through both files in O(n+m) time.", explanation: "No need to load entire files into memory.", hint: "For huge files, avoid sorting on the fly if they are already sorted.", level: "expert", codeExample: "comm already_sorted1 already_sorted2" },
  { question: "What is the purpose of `--total` option (GNU comm)?", shortAnswer: "Prints a summary line with counts of lines in each column.", explanation: "Shows how many lines are in column1, column2, column3.", hint: "Example: `comm --total file1 file2`.", level: "advanced", codeExample: "comm --total -23 file1 file2   # counts only unique in file1" },
  { question: "How to use `comm` with standard input? (e.g., `comm - <(sort file2)`)", shortAnswer: "Use `-` as filename for stdin: `sort file1 | comm - <(sort file2)`.", explanation: "The dash reads from stdin as first file.", hint: "Combine with pipes.", level: "advanced", codeExample: "grep 'pattern' file1 | sort | comm -23 - <(sort file2)" },
  { question: "Can `comm` handle files encoded with UTF‑8 special characters?", shortAnswer: "Yes, comm works on byte level; if sorting is correct, it works.", explanation: "But be careful of locale: use `LC_ALL=C sort` for deterministic order.", hint: "Ensure sort uses same locale as comm.", level: "advanced", codeExample: "LC_ALL=C comm <(LC_ALL=C sort file1) <(LC_ALL=C sort file2)" },
  { question: "How to find lines that are present in one file but not another, ignoring line order differences (after sorting)?", shortAnswer: "Sort both files first, then `comm -23`.", explanation: "Sorting normalises order; comm shows difference based on content.", hint: "Exactly the purpose of comm.", level: "basic", codeExample: "comm -23 <(sort file1) <(sort file2)" },
  { question: "What is the difference between `comm` and `diff` for sorted files?", shortAnswer: "`comm` focuses on set membership (columns of lines), `diff` shows changes in context.", explanation: "Comm is for line‑based set operations; diff for editing.", hint: "Use comm when you want 'which lines are common/different'.", level: "intermediate", codeExample: "# comm: columnar; diff: patch format" },
  { question: "How to check if two sorted files are identical using `comm`?", shortAnswer: "`comm -12 file1 file2` should output all lines; or `comm -23` and `-13` should be empty.", explanation: "If no lines are unique, the files are identical set‑wise (if no duplicates).", hint: "Use `diff -q` for quick check.", level: "basic", codeExample: "if [ -z \"$(comm -23 file1 file2)\" ] && [ -z \"$(comm -13 file1 file2)\" ]; then echo 'same'; fi" },
  { question: "What does the `-i` option do in `comm` (if any)?", shortAnswer: "Standard comm does not have `-i`. Case‑insensitive requires pre‑processing.", explanation: "GNU comm has no case‑insensitive flag.", hint: "Use `tr` or `sed` to convert case.", level: "basic", codeExample: "# Not available" },
  { question: "How to compare two files but ignore a certain prefix (e.g., line numbers) using comm?", shortAnswer: "Use `sed` to strip the prefix before comparing.", explanation: "Example: `sed 's/^[0-9]\\+ //' file1 | sort > file1_stripped`", hint: "Keep original files intact.", level: "expert", codeExample: "comm <(sed 's/^[0-9]\\+ //' file1 | sort) <(sed 's/^[0-9]\\+ //' file2 | sort)" },
  { question: "How to find lines that are common in three files using comm?", shortAnswer: "Pairwise: `comm -12 file1 file2 > common12` then `comm -12 common12 file3`.", explanation: "comm works on two files only; chain them.", hint: "Use intermediate temporary files.", level: "advanced", codeExample: "comm -12 f1 f2 | comm -12 - f3" },
  { question: "What is the memory usage of `comm` on very large files?", shortAnswer: "Low; it only holds one line from each file at a time.", explanation: "Streaming algorithm, O(1) memory.", hint: "Suitable for terabyte sorted files.", level: "expert", codeExample: "comm big1 big2   # efficient" },
  { question: "How to output only the second column (lines only in file2) without leading tab?", shortAnswer: "Use `comm -13 file1 file2 | sed 's/^\\t//'`.", explanation: "Column 2 output is indented with one tab; remove it.", hint: "Or use `comm -1 -3` (same as `-13`).", level: "intermediate", codeExample: "comm -13 f1 f2 | cut -f2" },
  { question: "Can `comm` be used to compare two directories by file names?", shortAnswer: "Yes: create sorted lists of filenames, then comm.", explanation: "Example: `comm -23 <(ls dir1 | sort) <(ls dir2 | sort)` to see files only in dir1.", hint: "Use `find` for recursive comparison.", level: "basic", codeExample: "comm -23 <(ls dir1) <(ls dir2)" }
];

export default questions;