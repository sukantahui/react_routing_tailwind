const questions = [
  { question: "What is the basic syntax of `join`?", shortAnswer: "`join file1 file2` (assumes both sorted on first field).", explanation: "Performs inner join on first field (whitespace separated).", hint: "Files must be sorted on the join field.", level: "basic", codeExample: "join students.txt scores.txt" },
  { question: "What happens if input files are not sorted on the join field?", shortAnswer: "Join will produce incorrect/missing matches, or no output at all.", explanation: "Join relies on sorted order to merge efficiently; unsorted input leads to undefined behavior.", hint: "Always sort before join.", level: "basic", codeExample: "sort -k1,1 file1 | join - file2" },
  { question: "How to join files on a field other than the first?", shortAnswer: "Use `-1 FIELD -2 FIELD` (1‑based indexing). Example: `join -1 2 -2 1 file1 file2`", explanation: "Field numbers refer to fields within each file (default whitespace delimiter).", hint: "Specify custom delimiter with `-t` if needed.", level: "intermediate", codeExample: "join -1 2 -2 3 fileA fileB" },
  { question: "How to set a custom delimiter for join (e.g., comma for CSV)?", shortAnswer: "`join -t ',' file1.csv file2.csv`.", explanation: "The delimiter must be a single character.", hint: "Spaces are default; if your file uses spaces, you may not need `-t`.", level: "basic", codeExample: "join -t '|' file1.txt file2.txt" },
  { question: "What does `join -a1` do?", shortAnswer: "Prints lines from file1 even if there's no matching line in file2 (left outer join).", explanation: "Missing fields from file2 are output as empty (or replaced via `-e`).", hint: "Combine with `-a2` for full outer join.", level: "intermediate", codeExample: "join -a1 file1 file2" },
  { question: "How to perform a full outer join (union of keys) with join?", shortAnswer: "`join -a1 -a2 file1 file2`.", explanation: "Prints all keys from both files, with missing fields left blank.", hint: "Use `-e 'NULL'` to replace empty fields.", level: "advanced", codeExample: "join -a1 -a2 -e 'NULL' file1 file2" },
  { question: "How to select specific fields in the output (not all fields)?", shortAnswer: "`join -o 1.2,2.3,1.1 ...` (file.field indices).", explanation: "`1.2` means field 2 from file1, `2.3` field 3 from file2, `0` means join field from either file.", hint: "Use `-o 0` to output join field only once.", level: "advanced", codeExample: "join -o 1.2,2.2 -t',' file1 file2" },
  { question: "How to show only lines that do NOT match? (anti‑join)", shortAnswer: "`join -v1 file1 file2` (lines in file1 not in file2); `-v2` for opposite.", explanation: "`-v` selects unmatched lines from the specified file.", hint: "Use `-v1 -v2` to show all unpaired lines from both.", level: "intermediate", codeExample: "join -v1 file1 file2" },
  { question: "What is the default field separator for join?", shortAnswer: "Whitespace (one or more spaces or tabs).", explanation: "Multiple spaces are treated as a single delimiter; leading/trailing whitespace is ignored.", hint: "Use `-t` to change to a different delimiter.", level: "basic", codeExample: "join file1 file2 # uses whitespace" },
  { question: "How to join two files where the key appears multiple times in one file?", shortAnswer: "Join will produce all combinations (cartesian product) for that key.", explanation: "Example: if key 101 appears twice in file1 and three times in file2, output will have 6 lines for that key.", hint: "Be cautious of exponential output.", level: "advanced", codeExample: "join file1 file2   # may produce many lines" },
  { question: "How to ignore case when joining?", shortAnswer: "Pre‑process keys: `tr '[:upper:]' '[:lower:]'` before sorting and joining.", explanation: "Join is case‑sensitive; convert both files to same case.", hint: "Use `sed 's/.*/\\L&/'` (GNU sed) to lowercase entire line.", level: "advanced", codeExample: "join <(tr 'A-Z' 'a-z' < file1 | sort) <(tr 'A-Z' 'a-z' < file2 | sort)" },
  { question: "What does `-e STRING` do in join?", shortAnswer: "Replaces empty output fields (from missing matches) with STRING.", explanation: "Used with `-a` or when fields are missing due to incomplete matches.", hint: "Example: `join -a1 -e 'N/A' file1 file2`.", level: "advanced", codeExample: "join -a1 -e '0' -o 1.1,2.2 file1 file2" },
  { question: "How to join two files on a field that contains spaces (e.g., 'John Doe')?", shortAnswer: "Difficult because fields are whitespace‑separated; pre‑process with `awk` to escape or change delimiter.", explanation: "Join cannot handle multi‑word fields as a single key if they contain spaces.", hint: "Use a different delimiter, e.g., tab, or convert spaces to underscores.", level: "expert", codeExample: "sed 's/ /_/g' file | sort | join - sed ..." },
  { question: "Can `join` process standard input? If so, how?", shortAnswer: "Yes, use `-` as filename: `join file1 -` or `join - file2`.", explanation: "The dash represents stdin, allowing pipelines.", hint: "Example: `sort file1 | join - file2`.", level: "intermediate", codeExample: "join -t',' file1.csv - < file2.csv" },
  { question: "What is the difference between `-a` and `-v` in join?", shortAnswer: "`-a` includes all lines from a file (including matched and unmatched); `-v` includes only unmatched lines (excludes matched).", explanation: "`-a1` = left outer join; `-v1` = lines only in file1 (difference).", hint: "`-a1` shows lines with empty fields for missing match; `-v1` shows only the original line (no join fields).", level: "intermediate", codeExample: "join -a1 file1 file2   # left outer\njoin -v1 file1 file2   # anti-join" },
  { question: "How to join files on a key that is not at the beginning of the line?", shortAnswer: "Use `-1` and `-2` to specify field numbers, but the key must be a single field; if key is embedded, you may need `awk` to extract it first.", explanation: "Join cannot handle multi‑field keys directly without pre‑processing.", hint: "Create a temporary file with key as first field using `awk`.", level: "expert", codeExample: "awk '{print $3,$0}' file1 | sort | join - file2" },
  { question: "Why does join sometimes output extra spaces or missing fields?", shortAnswer: "Due to inconsistent delimiters (mixing spaces and tabs) or leading/trailing spaces.", explanation: "Normalise delimiters before sorting.", hint: "Use `sed 's/^[[:space:]]*//; s/[[:space:]]+/ /g'` to standardise whitespace.", level: "advanced", codeExample: "# Pre-process: sed 's/  */ /g' file" },
  { question: "How to join files where the key contains a colon (`:`) but you use `-t ':'`?", shortAnswer: "That's fine, the colon is the delimiter; the first field is the key.", explanation: "Any single character can be delimiter, including colon.", hint: "But if the key itself could contain colon, it's problematic.", level: "basic", codeExample: "join -t ':' file1 file2" },
  { question: "What is the purpose of `--header` option in join (GNU extension)?", shortAnswer: "Treats the first line of each file as a header, not as data line.", explanation: "Useful for CSV with headers, prevents joining on header line.", hint: "Example: `join --header -t',' file1.csv file2.csv`.", level: "advanced", codeExample: "join --header -t '|' file1 file2" },
  { question: "How to join two files on multiple fields (composite key)?", shortAnswer: "Concatenate the fields into a single key with a separator, then join on that key.", explanation: "Example: `awk '{print $1\"|\"$2, $0}' file1 | sort` and similarly for file2.", hint: "Use a delimiter that doesn't appear in data (e.g., `\\x01`).", level: "expert", codeExample: "join <(awk '{print $1\"|\"$2,$0}' file1 | sort) <(awk '{print $1\"|\"$2,$0}' file2 | sort)" },
  { question: "What does `-i` do in join (if available)?", shortAnswer: "Some join implementations have `-i` for case‑insensitive compare, but GNU join does not (use pre‑processing).", explanation: "Check `man join` on your system.", hint: "Use `tr` for portability.", level: "advanced", codeExample: "# Not standard" },
  { question: "How to join two large files that are already sorted on disk efficiently?", shortAnswer: "Just run `join` directly – it streams both files and runs in O(n+m).", explanation: "No need to load entire files into memory; very efficient.", hint: "Use `-t` and `-o` to control output.", level: "expert", codeExample: "join already_sorted1 already_sorted2" },
  { question: "How to join files where the key field is the same number but different columns (e.g., file1 field3, file2 field1)?", shortAnswer: "`join -1 3 -2 1 file1 file2`.", explanation: "Specify the field numbers.", hint: "Ensure both files are sorted on that field, not necessarily field1.", level: "intermediate", codeExample: "join -1 3 -2 1 file1 file2" },
  { question: "What is the exit status of join?", shortAnswer: "0 on success, >0 on error. It does not distinguish ‘no matches’ vs matches; only I/O errors cause non‑zero exit.", explanation: "Unlike diff, join doesn't exit 1 when no matches found.", hint: "Check output rather than exit code for empty join.", level: "basic", codeExample: "if join file1 file2 >/dev/null; then echo 'command succeeded'; fi" },
  { question: "How to join files but suppress output of the join key (print it only once)?", shortAnswer: "Use `-o 0` for the key field, plus other fields: `join -o 0,1.2,2.2 file1 file2`.", explanation: "`0` represents the join field and prints it only once.", hint: "Default output prints join field twice; `-o` helps clean it up.", level: "advanced", codeExample: "join -o 0,2.2,1.3 file1 file2" },
  { question: "Why does join sometimes report `file1 is not in sorted order`?", shortAnswer: "The file is not sorted on the join field according to the collating order (locale).", explanation: "Sort with `LC_ALL=C sort` to ensure byte‑wise order.", hint: "Set `LC_ALL=C` before sort and join for consistent results.", level: "intermediate", codeExample: "LC_ALL=C sort file1 | LC_ALL=C join - file2" },
  { question: "How to join files where the key field contains missing values (empty field) and you want to treat them as keys?", shortAnswer: "Join treats empty field as a key (empty string). But sorting of empty lines may be ambiguous.", explanation: "Pre‑process to replace empty fields with a placeholder like 'NULL'.", hint: "Use `sed 's/^,/NULL,/g'` for CSV.", level: "expert", codeExample: "sed 's/,,/,NULL,/g' file.csv | sort | join ..." },
  { question: "Can join handle very wide records (many columns)?", shortAnswer: "Yes, join reads lines and writes lines; there's no practical limit on number of fields.", explanation: "Memory usage per line depends on line length.", hint: "For very wide records, ensure adequate memory.", level: "expert", codeExample: "join wide1 wide2" },
  { question: "How to join three files together using join?", shortAnswer: "Chain joins: `join file1 file2 | join - file3`.", explanation: "Output of first join becomes input for second (must be sorted on key).", hint: "Use `-o` to control which fields from intermediate join carry forward.", level: "advanced", codeExample: "join file1 file2 | sort -k1,1 | join - file3" },
  { question: "What is the difference between `join` and `paste`?", shortAnswer: "`join` matches lines by key and combines fields; `paste` concatenates lines side‑by‑side regardless of key.", explanation: "Join is like relational JOIN; paste is like placing files side‑by‑side.", hint: "Use join for keyed merging, paste for simple column alignment.", level: "basic", codeExample: "# join: based on common ID; paste: line must correspond exactly" }
];

export default questions;