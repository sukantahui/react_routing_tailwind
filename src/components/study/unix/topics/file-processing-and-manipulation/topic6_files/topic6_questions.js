const questions = [
  { question: "How to sort a file by the second column (field)?", shortAnswer: "`sort -k2,2 file.txt`", explanation: "`-k2,2` selects the second field (default whitespace delimiter) as the sort key.", hint: "Always specify both start and end for exact column.", level: "basic", codeExample: "sort -k2,2 data.txt" },
  { question: "What is the difference between `-k2` and `-k2,2`?", shortAnswer: "`-k2` sorts from field 2 to end of line; `-k2,2` sorts only field 2.", explanation: "`-k2` may include later fields, affecting tie‑breaking.", hint: "Use `-k2,2` for single‑column sort.", level: "intermediate", codeExample: "# Compare: sort -k2 vs sort -k2,2" },
  { question: "How to sort by column 3 numerically in descending order?", shortAnswer: "`sort -k3,3nr file`.", explanation: "`-n` for numeric, `-r` for reverse; attach flags immediately after key.", hint: "Order matters: `-k3,3rn` also works.", level: "basic", codeExample: "sort -k2,2nr scores.txt" },
  { question: "How to sort a CSV file by the second column (comma as delimiter)?", shortAnswer: "`sort -t',' -k2,2 data.csv`.", explanation: "`-t','` sets comma as field separator.", hint: "Make sure no spaces after commas in CSV.", level: "intermediate", codeExample: "sort -t',' -k3,3n student.csv" },
  { question: "How to sort by multiple columns (e.g., department then salary)?", shortAnswer: "`sort -k1,1 -k3,3n employees.txt`.", explanation: "First key is department (field 1), second is salary numeric (field 3).", hint: "Order of `-k` options defines precedence.", level: "intermediate", codeExample: "sort -k2,2 -k1,1 data.txt" },
  { question: "How to sort by a column but ignore leading spaces in that column?", shortAnswer: "Use `-b` flag either globally or inside key: `-k2,2b`.", explanation: "`-b` ignores leading blanks when determining field start.", hint: "Useful for irregularly aligned columns.", level: "advanced", codeExample: "sort -k2,2b messy.txt" },
  { question: "What does `-k1.2,1.4` mean in sort?", shortAnswer: "Sorts by characters 2‑4 of the first field (not whole field).", explanation: "`.` allows character‑level indexing within a field.", hint: "Rarely used but powerful for fixed‑width data.", level: "expert", codeExample: "cut -c2-4 file | sort" },
  { question: "How to sort by month name (e.g., Jan, Feb, Mar)?", shortAnswer: "`sort -k1,1M dates.txt`.", explanation: "`-M` sorts by month name (three‑letter abbreviation).", hint: "Works with full month names as well (January, February).", level: "advanced", codeExample: "sort -k3,3M log.txt" },
  { question: "What does `global` vs `key‑specific` options mean in sort?", shortAnswer: "Global options like `-n` apply to all keys. Key‑specific like `-k2,2n` apply only to that key.", explanation: "Mix both: `sort -n -k2` (global numeric) vs `sort -k2,2n` (numeric only on field2).", hint: "Key‑specific overrides global for that key.", level: "advanced", codeExample: "sort -n -k2,2 -k3,3 file  # numeric on both keys" },
  { question: "How to sort IP addresses correctly (dotted quad)?", shortAnswer: "`sort -t. -k1,1n -k2,2n -k3,3n -k4,4n ips.txt`.", explanation: "Treat each octet as numeric field separated by `-t.`.", hint: "Also possible with `-V` (version sort) for simple cases.", level: "expert", codeExample: "sort -t. -k1,1n -k2,2n -k3,3n -k4,4n ips.txt" },
  { question: "How to sort by version numbers (1.2, 1.10, 2.0)?", shortAnswer: "`sort -V versions.txt`.", explanation: "`-V` handles version‑aware sorting (GNU).", hint: "Better than `-n` for dotted versions.", level: "advanced", codeExample: "sort -V pkglist.txt" },
  { question: "What does `--debug` option in sort show?", shortAnswer: "Prints detailed information about key extraction and comparisons.", explanation: "Helps understand why sorting results are as they are.", hint: "Useful for debugging complex `-k` specifications.", level: "expert", codeExample: "sort --debug -k2,2 file.txt" },
  { question: "How to sort a file by multiple keys, but with different directions per key?", shortAnswer: "Add `r` to specific keys: `sort -k1,1 -k2,2nr`.", explanation: "First key ascending (default), second numeric descending.", hint: "Equivalent to `--key=1,1 --key=2,2nr`.", level: "intermediate", codeExample: "sort -k1,1 -k3,3nr data.txt" },
  { question: "How to sort by a column and also remove duplicates based on that column?", shortAnswer: "Use `-u` with sort, but note `-u` applies to entire line, not column. To dedup by column, use `sort | uniq` or `awk`.", explanation: "`sort -u` removes duplicate lines; not column‑based dedup.", hint: "For column unique, use `sort -k1,1 | uniq -f0` or `awk`.", level: "advanced", codeExample: "sort -u -k2,2 file.txt   # wrong: whole line dedup" },
  { question: "How to sort by a column that has numeric values with units (e.g., 10K, 5M)?", shortAnswer: "Use `sort -h` (human‑numeric) on that column: `sort -k2,2h`.", explanation: "Handles K, M, G suffixes.", hint: "Avoid mixing with `-n`.", level: "advanced", codeExample: "sort -k2,2h sizes.txt" },
  { question: "How to sort a file by a column but only when lines match a pattern?", shortAnswer: "Pre‑filter with `grep` before sorting: `grep 'pattern' file | sort -k2,2`.", explanation: "Sort sees only matching lines.", hint: "Or use `sed` to remove non‑matching lines.", level: "intermediate", codeExample: "grep 'ERROR' log.txt | sort -k4,4" },
  { question: "Why does `sort -k2,2n` sometimes mis‑sort numbers with leading zeros?", shortAnswer: "Leading zeros are fine; `-n` ignores them. If still wrong, check field delimiter or extra spaces.", explanation: "`-n` interprets `010` as 10, correct.", hint: "Use `-g` for general numeric if issues.", level: "basic", codeExample: "echo '010\n2' | sort -n   # 2,10" },
  { question: "How to sort by a column that contains NULL or empty values?", shortAnswer: "Empty fields sort to the beginning (default) or end with `-r`.", explanation: "Empty strings are less than any character.", hint: "Use `--debug` to see how empty fields are compared.", level: "advanced", codeExample: "echo 'a\n\nb' | sort   # blank line first" },
  { question: "How to sort by a column that has both numbers and text (mixed types)?", shortAnswer: "Mixed types cause lexicographic order; use `-n` to force numeric, but non‑numeric will be treated as zero.", explanation: "Better to separate data or use `awk` to convert.", hint: "Preprocess with `sed` to prefix zeros for numbers.", level: "expert", codeExample: "# Not reliable; redesign data" },
  { question: "What is the purpose of `-s` when sorting by multiple columns?", shortAnswer: "Makes sort stable: preserves original order for lines that compare equal in all keys.", explanation: "Without `-s`, order of ties is unspecified.", hint: "Used with `-k` to guarantee deterministic results.", level: "advanced", codeExample: "sort -s -k2,2 -k1,1 file" },
  { question: "How to sort by a column that uses a multi‑character delimiter (e.g., `::`)?", shortAnswer: "Use `awk` to convert to single‑char delimiter, then sort. Example: `awk -F'::' '{print $2}' file`.", explanation: "`-t` only accepts single character.", hint: "`sed 's/::/|/g' file | sort -t'|' -k2`", level: "expert", codeExample: "sed 's/::/|/g' data | sort -t'|' -k2" },
  { question: "How to sort a file by column but ignore case in that column only?", shortAnswer: "Use `-f` inside key: `-k2,2f`.", explanation: "`-f` folds case for that key only.", hint: "Global `-f` also works but affects all keys.", level: "advanced", codeExample: "sort -k2,2f words.txt" },
  { question: "How to sort by a column that contains dates in DD/MM/YYYY format?", shortAnswer: "Convert to YYYYMMDD then sort: `awk '{split($3,d,\"/\"); print d[3] d[2] d[1], $0}' file | sort | cut -d' ' -f2-`", explanation: "Complex; use external tools.", hint: "Or use `date` to convert to epoch.", level: "expert", codeExample: "# Too complex for one command" },
  { question: "Why does `sort -k2,2n` sometimes treat `-1` as `0`?", shortAnswer: "`-n` does handle negative numbers correctly. Check if there are non‑numeric characters.", explanation: "If a line has `-1` with a trailing space or other char, it may be considered invalid.", hint: "Use `--debug` to diagnose.", level: "advanced", codeExample: "echo '-1\n2' | sort -n   # -1,2" },
  { question: "How to sort by a column that is not the first column but has leading spaces in the line?", shortAnswer: "Use `-b` to ignore leading blanks when determining field start.", explanation: "`-k2,2b` will ignore spaces before field 2.", hint: "Also `-t$'\\t'` if using tabs.", level: "intermediate", codeExample: "sort -k2,2b messy.txt" },
  { question: "How to sort by column and also output only that column?", shortAnswer: "Pipe sorted output to `cut`: `sort -k2,2 file | cut -f2`.", explanation: "Sort then select.", hint: "Use `--output-delimiter` if needed.", level: "basic", codeExample: "sort -k3,3n data | cut -f3" },
  { question: "How to sort a huge file by a column without loading entire file into memory?", shortAnswer: "`sort` automatically uses external temporary files. Specify `-T` for directory.", explanation: "`sort` is designed for large files.", hint: "Use `-S` for buffer size, `-T` for temp location.", level: "expert", codeExample: "sort -T /bigtmp -k2,2 huge.csv" },
  { question: "What is the difference between sorting by `-k2` and `-k2,2` when lines have variable number of fields?", shortAnswer: "`-k2` will compare from field2 to line end, so if field3 differs, it breaks ties; `-k2,2` ignores later fields.", explanation: "Choose based on whether later fields should influence order.", hint: "Usually want `-k2,2`.", level: "advanced", codeExample: "echo 'a 1 9\nb 1 8' | sort -k2   # 1 9 then 1 8? may differ" },
  { question: "How to sort by column but treat the entire line as secondary key?", shortAnswer: "Use `-k2,2 -s` so that primary key is column2, then stable sort preserves input order for ties.", explanation: "`-s` with a single key makes the whole line act as implicit secondary key (original order).", hint: "Use `-s` for deterministic results.", level: "expert", codeExample: "sort -k2,2 -s file" },
  { question: "How to sort by column and then remove duplicate lines based on that column only?", shortAnswer: "Use `awk` to print only first occurrence of each key: `sort -k2,2 file | awk '!seen[$2]++'`.", explanation: "`sort` arranges, `awk` filters by first occurrence.", hint: "Works with any delimiter by adjusting `$2`.", level: "expert", codeExample: "sort -t',' -k3,3n data.csv | awk -F',' '!seen[$3]++'" },
  { question: "How to sort mixed alphanumeric strings (e.g., file1, file10, file2) naturally?", shortAnswer: "Use `sort -V` (version sort) which handles numbers inside strings.", explanation: "`-V` splits string into numeric and non‑numeric parts.", hint: "Also `ls -v` does similar.", level: "advanced", codeExample: "echo -e 'file1\nfile10\nfile2' | sort -V" }
];

export default questions;