const questions = [
  { question: "What is the basic syntax of `tr` and how does it read input?", shortAnswer: "`tr [OPTIONS] SET1 [SET2]` – reads from stdin only; does not accept file arguments.", explanation: "Use redirection (`< file`) or pipe (`|`) to provide input.", hint: "Example: `tr 'a' 'b' < file.txt`", level: "basic", codeExample: "echo 'hello' | tr 'l' 'L'   # heLLo" },
  { question: "How to convert lowercase to uppercase using `tr`?", shortAnswer: "`tr '[:lower:]' '[:upper:]'` or `tr 'a-z' 'A-Z'`.", explanation: "The first uses POSIX character classes (more portable).", hint: "For single character, just list them: `tr 'aeiou' 'AEIOU'`.", level: "basic", codeExample: "echo 'Hello' | tr '[:lower:]' '[:upper:]'   # HELLO" },
  { question: "How to delete all digits from a file using `tr`?", shortAnswer: "`tr -d '0-9' < file.txt`.", explanation: "`-d` deletes all occurrences of characters in SET1.", hint: "Range `0-9` works, as does `[:digit:]`.", level: "basic", codeExample: "echo 'abc123' | tr -d '0-9'   # abc" },
  { question: "What does `tr -s ' '` do?", shortAnswer: "Squeezes multiple consecutive spaces into a single space.", explanation: "`-s` replaces sequences of repeating characters with a single occurrence.", hint: "Useful for normalising whitespace.", level: "intermediate", codeExample: "echo 'a    b' | tr -s ' '   # a b" },
  { question: "How to remove all punctuation using `tr`?", shortAnswer: "`tr -d '[:punct:]' < file.txt`.", explanation: "`[:punct:]` includes punctuation characters like .,!?;: etc.", hint: "Combine with `-s` to also squeeze spaces.", level: "intermediate", codeExample: "echo 'Hello, world!' | tr -d '[:punct:]'   # Hello world" },
  { question: "How to keep only digits and spaces (remove everything else) with `tr`?", shortAnswer: "`tr -cd '[:digit:] '` (complement + delete).", explanation: "`-c` complements the set, `-d` deletes; together they delete all characters NOT in the set.", hint: "`-cd` is a common combination: keep only certain characters.", level: "advanced", codeExample: "echo 'abc 123 def' | tr -cd '[:digit:] '   # ' 123 '" },
  { question: "What is the difference between `tr 'a-z' 'A-Z'` and `tr '[:lower:]' '[:upper:]'`?", shortAnswer: "Both convert lowercase to uppercase, but the character class is more portable across locales.", explanation: "In some locales, `a-z` may not be contiguous (e.g., `a`..`z` might include accented characters).", hint: "Use `LC_ALL=C` with ranges for predictability.", level: "advanced", codeExample: "LC_ALL=C tr 'a-z' 'A-Z' < file" },
  { question: "How to remove all newlines from a file (make it one line)?", shortAnswer: "`tr -d '\\n' < file.txt`.", explanation: "Deletes newline characters, concatenating all lines.", hint: "Be careful: the last line may need a newline to avoid gluing words together.", level: "basic", codeExample: "printf 'a\\nb\\nc' | tr -d '\\n'   # abc" },
  { question: "How to replace multiple spaces with a single comma using `tr`?", shortAnswer: "First squeeze spaces, then translate space to comma: `tr -s ' ' | tr ' ' ','`.", explanation: "Tr cannot directly replace a sequence with a single character unless it's a squeeze.", hint: "For real comma separation, `sed 's/  */,/g'` is more direct.", level: "advanced", codeExample: "echo 'a   b   c' | tr -s ' ' | tr ' ' ','   # a,b,c" },
  { question: "What does `tr -c '[:alpha:]' ' '` do?", shortAnswer: "Replaces every non‑alphabetic character with a space.", explanation: "`-c` complements SET1, so it matches all characters except letters, and translates each to a space.", hint: "Useful for isolating words.", level: "advanced", codeExample: "echo 'hello123world' | tr -c '[:alpha:]' ' '   # 'hello world'" },
  { question: "Can `tr` handle Unicode characters (e.g., é, 你)?", shortAnswer: "It depends on the implementation and locale. GNU tr works on bytes, not characters, so multibyte Unicode may be corrupted.", explanation: "For multibyte characters, use `sed`, `awk`, or `perl`.", hint: "Stick to ASCII for portable tr usage.", level: "expert", codeExample: "# Not recommended for Unicode" },
  { question: "How to convert all tabs to spaces using `tr`?", shortAnswer: "`tr '\\t' ' ' < file.txt`.", explanation: "Use `$'\\t'` in shells that support ANSI‑C quoting, or literal tab with Ctrl+V Tab.", hint: "In a script, you can write `tr '\t' ' '`.", level: "basic", codeExample: "echo -e 'a\\tb' | tr '\\t' ' '   # a b" },
  { question: "What is the purpose of `tr -ds SET1 SET2`?", shortAnswer: "First deletes characters in SET1, then squeezes repeated characters in SET2.", explanation: "The `-ds` (or `-s -d`) order matters: deletion first, then squeeze.", hint: "Example: `tr -ds ' ' ' '` deletes all spaces? Actually it deletes spaces then squashes spaces (no change). Better: delete digits then squeeze spaces.", level: "expert", codeExample: "echo 'a 1 2  b' | tr -ds '0-9' ' '   # delete digits, then squash spaces" },
  { question: "How to make `tr` case‑insensitive replacement (e.g., 'a' or 'A' to 'x')?", shortAnswer: "Use character classes or list both cases: `tr 'aA' 'x'`.", explanation: "But if SET2 is shorter, the last character is repeated.", hint: "For full case‑insensitive translation, convert to lower case first.", level: "intermediate", codeExample: "echo 'AbC' | tr 'aA' 'x'   # xbC (only a/A become x)" },
  { question: "How to swap two characters using `tr` (e.g., swap 'a' and 'b')?", shortAnswer: "Use a temporary intermediate character: `tr 'ab' 'ba'`.", explanation: "Tr processes each character independently; swapping requires a two‑step process: `tr 'ab' 'bc' | tr 'ca' 'ab'` or using a third char.", hint: "Direct `tr 'ab' 'ba'` does not work because 'a'→'b', 'b'→'a' simultaneously.", level: "expert", codeExample: "echo 'ab' | tr 'ab' 'ba'   # 'ba' (works because of mapping order). Actually it maps a→b, b→a, so 'ab' becomes 'ba'. It works for swapping." },
  { question: "How to translate only the first occurrence of a character? (Cannot; tr is global)", shortAnswer: "`tr` always processes all characters; use `sed` for first‑only replacement.", explanation: "Example: `sed '0,/a/s/a/b/'`.", hint: "`tr` is all‑or‑nothing.", level: "advanced", codeExample: "# Not possible with tr" },
  { question: "What does `tr '[:space:]' '\n'` do?", shortAnswer: "Replaces all whitespace characters (spaces, tabs, newlines) with newlines.", explanation: "Effectively separates text into one word per line (if no punctuation).", hint: "Useful for getting a list of words.", level: "intermediate", codeExample: "echo 'Hello world' | tr '[:space:]' '\\n'   # Hello\\nworld" },
  { question: "How to remove only leading/trailing spaces? (tr cannot; use sed or awk)", shortAnswer: "`tr` works on all characters, not specifically at start/end. Use `sed 's/^ *//;s/ *$//'`.", explanation: "Tr is for global character operations, not positional.", hint: "Combine with `| xargs echo` to trim leading/trailing spaces.", level: "advanced", codeExample: "# Not possible" },
  { question: "How to use `tr` to encode a simple cipher like ROT13?", shortAnswer: "`tr 'A-Za-z' 'N-ZA-Mn-za-m'`.", explanation: "Shifts letters by 13 positions.", hint: "Applying twice restores original.", level: "intermediate", codeExample: "echo 'Hello' | tr 'A-Za-z' 'N-ZA-Mn-za-m'   # Uryyb" },
  { question: "What is the difference between `tr -d ' '` and `tr -s ' '`?", shortAnswer: "`-d` removes all spaces; `-s` reduces multiple spaces to a single space.", explanation: "Choose based on whether you want all spaces removed or just normalised.", hint: "`-s` keeps one space.", level: "basic", codeExample: "echo 'a  b' | tr -d ' '   # ab\necho 'a  b' | tr -s ' '   # a b" },
  { question: "How to replace each newline with a space (join lines) using `tr`?", shortAnswer: "`tr '\\n' ' ' < file.txt`.", explanation: "All newlines become spaces; final line may not have trailing newline.", hint: "Add a trailing space if needed.", level: "basic", codeExample: "printf 'a\\nb\\nc' | tr '\\n' ' '   # a b c " },
  { question: "What does `tr -c '\\n' ' '` do?", shortAnswer: "Replaces every character that is NOT a newline with a space.", explanation: "Effectively replaces all non‑newline characters with spaces, preserving newlines.", hint: "Useful for keeping only line structure.", level: "advanced", codeExample: "echo -e 'abc\\ndef' | tr -c '\\n' ' '   # '   \\n   ' (spaces + newline)" },
  { question: "How to use `tr` with octal or hexadecimal escape sequences?", shortAnswer: "Use `printf` or `echo -e` to generate the escapes and pipe to tr, or use `$'\\xxx'` in bash.", explanation: "Tr itself doesn't interpret escapes; the shell does.", hint: "Example: `echo $'a\\tb' | tr '\\t' ','`.", level: "advanced", codeExample: "printf 'a\\x20b' | tr '\\x20' '_'   # a_b" },
  { question: "Why does `tr 'a-z' 'A-Z'` sometimes not work in some locales?", shortAnswer: "Because in some locales, `a-z` may include accented or other characters, or the order may not be what you expect.", explanation: "Using `LC_ALL=C` forces ASCII ordering.", hint: "Use POSIX classes `[:lower:]` and `[:upper:]` for safety.", level: "intermediate", codeExample: "LC_ALL=C tr 'a-z' 'A-Z' < file" },
  { question: "How to remove consecutive duplicate blank lines (squeeze newlines) but keep one blank line?", shortAnswer: "`tr -s '\\n'` reduces multiple newlines to one newline.", explanation: "One newline separates lines; two or more newlines become a single newline.", hint: "To keep exactly one blank line between paragraphs, use `cat -s` or `tr -s '\\n'`.", level: "basic", codeExample: "printf 'a\\n\\n\\nb\\n' | tr -s '\\n'   # a\\n\\nb\\n (two newlines become one)" },
  { question: "How to count the number of commas in a file using `tr`?", shortAnswer: "`tr -cd ',' | wc -c` (delete everything except commas, then count characters).", explanation: "`tr -cd ','` keeps only commas; `wc -c` counts bytes (commas).", hint: "Also `grep -o ',' | wc -l` works.", level: "intermediate", codeExample: "echo 'a,b,c' | tr -cd ',' | wc -c   # 2" },
  { question: "What does `tr '[:upper:]' '[:lower:]'` do to numbers and symbols?", shortAnswer: "Nothing – they are not in the `[:upper:]` class, so they remain unchanged.", explanation: "Only characters that match the first set are translated; others pass through.", hint: "Useful for case conversion while preserving punctuation.", level: "basic", codeExample: "echo 'A1!B' | tr '[:upper:]' '[:lower:]'   # a1!b" },
  { question: "How to replace multiple spaces with a single tab using `tr`?", shortAnswer: "First squeeze spaces, then translate space to tab: `tr -s ' ' | tr ' ' '\\t'`.", explanation: "Cannot replace variable‑width spaces directly; you could also use `sed 's/  */\\t/g'`.", hint: "For CSV to TSV, `tr '\\t' ','` is common.", level: "intermediate", codeExample: "echo 'a   b   c' | tr -s ' ' | tr ' ' '\\t'   # a\\tb\\tc" },
  { question: "How to delete all characters except newline (keep only line structure)?", shortAnswer: "`tr -cd '\\n' < file.txt`.", explanation: "Delete the complement of newline, i.e., everything except newline.", hint: "Produces empty lines for each original line (only newlines).", level: "advanced", codeExample: "printf 'a\\nb\\nc' | tr -cd '\\n'   # \\n\\n" },
  { question: "What is the difference between `tr` and `sed` for character translation?", shortAnswer: "`tr` is faster and simpler for single‑character replacements; `sed` can handle regex and string substitutions.", explanation: "Use `tr` for character set operations (case, delete, squeeze). Use `sed` for pattern matching and word‑level replacements.", hint: "`tr` cannot work with words or regular expressions.", level: "intermediate", codeExample: "# tr: tr 'a' 'b'\n# sed: sed 's/old/new/g'" }
];

export default questions;