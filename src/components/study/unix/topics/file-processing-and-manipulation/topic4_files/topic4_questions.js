const questions = [
  { question: "What does `wc file.txt` output by default?", shortAnswer: "Three numbers: lines, words, characters (in that order).", explanation: "Output: `3 9 44 file.txt` means 3 lines, 9 words, 44 characters.", hint: "Order is always lines‑words‑characters.", level: "basic", codeExample: "wc data.txt" },
  { question: "How to count only lines in a file?", shortAnswer: "Use `wc -l file.txt`.", explanation: "`-l` stands for lines. Output is just the number of lines.", hint: "Often used in scripts to check file length.", level: "basic", codeExample: "line_count=$(wc -l < file.txt)" },
  { question: "What is the difference between `wc -c` and `wc -m`?", shortAnswer: "`-c` counts bytes, `-m` counts characters (multibyte safe).", explanation: "For ASCII they are equal. For UTF‑8, a character may be multiple bytes.", hint: "Use `-m` for human‑readable text.", level: "intermediate", codeExample: "echo 'αβγ' | wc -c   # 6 (bytes)\necho 'αβγ' | wc -m   # 3 (characters)" },
  { question: "How to count words in a file?", shortAnswer: "`wc -w file.txt`.", explanation: "Words are sequences of non‑whitespace separated by spaces, tabs, newlines.", hint: "Punctuation attached to words is included.", level: "basic", codeExample: "word_count=$(wc -w < essay.txt)" },
  { question: "What does `wc -L file.txt` show?", shortAnswer: "The length (in characters) of the longest line in the file.", explanation: "Useful for checking maximum line width in fixed‑width files.", hint: "If you need the line itself, combine with `awk`.", level: "intermediate", codeExample: "max_len=$(wc -L < wide_file.txt)" },
  { question: "How to count lines in all `.log` files and get a total?", shortAnswer: "`wc -l *.log` outputs line counts per file and a total line at the end.", explanation: "The last line is the sum of all lines.", hint: "Use `tail -1` to extract just the total.", level: "basic", codeExample: "total=$(wc -l *.log | tail -1 | awk '{print $1}')" },
  { question: "How to count lines without printing filenames?", shortAnswer: "Redirect file content via stdin: `wc -l < file.txt`.", explanation: "When using `<` redirection, wc doesn't know the filename, so it prints only the number.", hint: "Useful for storing count in a variable.", level: "basic", codeExample: "count=$(wc -l < data.txt)" },
  { question: "What is faster: `grep pattern file | wc -l` or `grep -c pattern file`?", shortAnswer: "`grep -c` is significantly faster because it doesn't spawn a separate process.", explanation: "`grep -c` counts matches internally without writing to a pipe.", hint: "Always use `grep -c` when possible.", level: "advanced", codeExample: "# Faster: grep -c 'ERROR' log.txt\n# Slower: grep 'ERROR' log.txt | wc -l" },
  { question: "How to count the number of files in a directory?", shortAnswer: "`ls -1 | wc -l`.", explanation: "`ls -1` lists one file per line; wc -l counts lines.", hint: "Hidden files not included; use `ls -a` or `find`.", level: "basic", codeExample: "file_count=$(ls -1 | wc -l)" },
  { question: "Does `wc -l` count the last line if it has no trailing newline?", shortAnswer: "No, it will be one less than expected. Many tools add a newline, but not all.", explanation: "wc counts newline characters; if the file ends without newline, that line isn't counted.", hint: "Use `cat -A file` to see if last line ends with `$`.", level: "advanced", codeExample: "printf 'hello' | wc -l   # 0 (no newline)\nprintf 'hello\\n' | wc -l   # 1" },
  { question: "How to count characters in a string (not a file)?", shortAnswer: "Use `echo -n 'string' | wc -m`.", explanation: "`-n` suppresses trailing newline; otherwise the newline would be counted.", hint: "For word count, use `echo 'string' | wc -w`.", level: "intermediate", codeExample: "len=$(echo -n 'Hello' | wc -c)   # 5" },
  { question: "What is the output format of `wc` on multiple files?", shortAnswer: "Each line shows filename and counts; last line shows 'total' and summed counts.", explanation: "Example: `3 9 44 file1.txt; 2 5 30 file2.txt; 5 14 74 total`", hint: "Use `tail -1` to get total line.", level: "basic", codeExample: "wc *.txt | tail -1" },
  { question: "How to count only the total number of bytes used by all `.log` files?", shortAnswer: "`cat *.log | wc -c`.", explanation: "`cat` concatenates all files, then `wc -c` counts total bytes.", hint: "Be careful: huge files may cause memory issues? wc streams, so safe.", level: "advanced", codeExample: "total_bytes=$(cat *.log | wc -c)" },
  { question: "Why does `wc -m` sometimes differ from `wc -c`?", shortAnswer: "Because `-m` counts multibyte characters as one, while `-c` counts each byte individually.", explanation: "UTF‑8 uses 1‑4 bytes per character.", hint: "For non‑English text, use `-m`.", level: "intermediate", codeExample: "echo 'こんにちは' | wc -c   # 15 (5 chars * 3 bytes)\necho 'こんにちは' | wc -m   # 5" },
  { question: "How to count the number of lines in a huge file quickly?", shortAnswer: "`wc -l hugefile.txt`. It's very fast (optimised in C).", explanation: "wc reads the file once, counting newline characters.", hint: "Avoid `cat hugefile | wc -l` (extra process).", level: "basic", codeExample: "time wc -l huge.csv" },
  { question: "Can wc count lines in compressed files (`.gz`) without decompressing?", shortAnswer: "No, wc cannot read compressed files. Use `zcat file.gz | wc -l`.", explanation: "`zcat` decompresses to stdout, then wc counts.", hint: "For `.bz2`, use `bzcat`.", level: "advanced", codeExample: "zcat huge.log.gz | wc -l" },
  { question: "What is the limit of numbers wc can output?", shortAnswer: "Up to the maximum line/word/byte count the filesystem can represent (very large).", explanation: "wc uses internal 64‑bit counters, can handle files larger than 2GB.", hint: "No practical limit.", level: "expert", codeExample: "# Works for multi‑terabyte files" },
  { question: "How to count words but treat hyphenated words as two words?", shortAnswer: "wc already does: 'state‑of‑the‑art' is counted as three words (split on hyphen).", explanation: "Hyphen is a separator; not a letter or digit, so it splits words.", hint: "To treat hyphen as part of word, replace hyphens with a letter then count.", level: "advanced", codeExample: "echo 'state-of-the-art' | wc -w   # 3" },
  { question: "How to get only the word count from wc output (strip leading spaces)?", shortAnswer: "Use `wc -w < file.txt` (stdin redirection) or `wc -w file.txt | awk '{print $1}'`.", explanation: "When filename is given, wc outputs leading spaces; redirect to avoid.", hint: "`wc -w file.txt | cut -d' ' -f1` also works.", level: "intermediate", codeExample: "words=$(wc -w < file.txt)" },
  { question: "What is the purpose of `--files0-from` in GNU wc?", shortAnswer: "Reads a list of filenames from stdin, separated by null characters.", explanation: "Useful when dealing with filenames containing newlines or special characters.", hint: "Used with `find -print0`.", level: "expert", codeExample: "find . -name '*.txt' -print0 | wc --files0-from=- -l" },
  { question: "How to count the number of characters in all lines except newlines?", shortAnswer: "`tr -d '\\n' < file.txt | wc -c`.", explanation: "Remove newlines with `tr`, then count characters.", hint: "Same as total file size minus line count.", level: "advanced", codeExample: "total_chars=$(tr -d '\\n' < file | wc -c)" },
  { question: "Why does `wc -w` count '123' as a word?", shortAnswer: "Because a word is any non‑whitespace sequence; numbers count as words.", explanation: "`wc` does not distinguish alphabetic words; it's purely based on delimiters.", hint: "To count only alphabetic words, use `grep -o '[A-Za-z]\\+' | wc -l`.", level: "basic", codeExample: "echo 'abc 123 def' | wc -w   # 3" },
  { question: "How to count lines in a Git repository excluding `.git` directory?", shortAnswer: "`git ls-files | xargs wc -l`.", explanation: "`git ls-files` lists tracked files; `xargs wc -l` counts lines.", hint: "Use `git ls-files | grep '.js$' | xargs wc -l` for specific languages.", level: "advanced", codeExample: "git ls-files | xargs wc -l | tail -1" },
  { question: "What does `wc -L` output for an empty file?", shortAnswer: "0 (since no lines, maximum length is zero).", explanation: "An empty file has no lines, so longest line length is 0.", hint: "Useful as sentinel.", level: "intermediate", codeExample: "touch empty; wc -L empty   # 0" },
  { question: "How to count the number of times a specific character appears in a file?", shortAnswer: "Use `tr -cd 'x' | wc -c` to count character 'x'.", explanation: "`tr -cd` deletes all characters except 'x', then wc counts remaining.", hint: "Example: `tr -cd 'e' < file | wc -c` counts 'e'.", level: "advanced", codeExample: "count_e=$(tr -cd 'e' < file.txt | wc -c)" },
  { question: "Why is `wc -l` output sometimes one less than expected?", shortAnswer: "Likely because the last line lacks a trailing newline character.", explanation: "wc counts newline characters; if the last line doesn't end with newline, it's not counted.", hint: "Use `sed -n '$=' file.txt` for POSIX‑compliant line count that works regardless.", level: "intermediate", codeExample: "printf 'no newline' | wc -l   # 0" },
  { question: "How to count lines in all `.txt` files recursively?", shortAnswer: "`find . -name '*.txt' -exec wc -l {} + | tail -1`.", explanation: "`find` with `-exec` collects files, wc sums them, `tail -1` gives total.", hint: "Use `-print0` and `xargs -0` for robustness.", level: "advanced", codeExample: "find . -type f -name '*.txt' -print0 | xargs -0 wc -l | tail -1" },
  { question: "What does the `total` line in wc output represent when multiple files are given?", shortAnswer: "The sum of each count (lines, words, characters) across all files.", explanation: "It's not the total of the totals, but the actual sum of each column.", hint: "Useful for project statistics.", level: "basic", codeExample: "wc -l *.txt | tail -1" },
  { question: "How to make wc output only numbers (no filenames or spaces)?", shortAnswer: "Redirect file content: `wc -l < file.txt`.", explanation: "Without a filename argument, wc outputs only the number (no spaces).", hint: "Best for assigning to variables.", level: "basic", codeExample: "count=$(wc -l < file.txt)" },
  { question: "What is the difference between `wc -w` and counting words with `awk '{print NF}'`?", shortAnswer: "Both count words similarly, but `awk` can be customised for word boundaries.", explanation: "`wc -w` is faster and standard; `awk` can handle special cases (e.g., hyphen as non‑delimiter).", hint: "For 99% of cases, `wc -w` is sufficient.", level: "expert", codeExample: "echo 'hello-world' | wc -w   # 1\n echo 'hello-world' | awk '{print NF}'   # also 1" }
];

export default questions;