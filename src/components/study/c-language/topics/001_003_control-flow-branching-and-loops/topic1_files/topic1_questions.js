const questions = [
  {
    question: "How do nested loops work in matrix and pattern algorithms?",
    shortAnswer: "The outer loop controls rows/iterations; the inner loop completes all iterations per outer row.",
    explanation: "For every single step of the outer loop index, the inner loop executes its full cycle from start to finish.",
    hint: "Outer loop = rows, Inner loop = columns/stars.",
    level: "basic"
  }
];

export default questions;
