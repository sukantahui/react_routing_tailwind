const questions = [
  {
    question: "What is base condition in recursion and why is it mandatory?",
    shortAnswer: "Base condition specifies the termination criteria that halts recursive function calls.",
    explanation: "Without a base condition, the function calls itself indefinitely, exhausting CPU stack space and causing a Stack Overflow crash.",
    hint: "Base condition stops recursion.",
    level: "basic"
  }
];

export default questions;
