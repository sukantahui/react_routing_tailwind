// topic4_files/topic4_questions.js
const questions = [
  {question:"What does the NOT operator (~) do?",shortAnswer:"Reverses the truth value of a proposition.",explanation:"If p is true, ~p is false; if p is false, ~p is true. It is a unary operator (applies to one proposition).",hint:"Think of it as a logical 'flip'.",level:"basic",codeExample:"let p = true; let notP = !p; // false"},
  {question:"When is p ∧ q true?",shortAnswer:"Only when both p and q are true.",explanation:"Conjunction requires all operands to be true. If any is false, the result is false. This matches the English 'and'.",hint:"AND = all must be true.",level:"basic",codeExample:"let canDrive = age>=18 && hasLicense;"},
  {question:"When is p ∨ q false?",shortAnswer:"Only when both p and q are false.",explanation:"Disjunction is false only in the case where every operand is false. Otherwise it's true (inclusive OR).",hint:"OR = at least one true.",level:"basic",codeExample:"let canEnter = isWeekend || isHoliday;"},
  {question:"What is the truth table for ~p?",shortAnswer:"p=T → ~p=F; p=F → ~p=T.",explanation:"Negation simply flips the truth value. It's the simplest operator.",hint:"Two rows only.",level:"basic",codeExample:"// Truth table:\n// p | ~p\n// T | F\n// F | T"},
  {question:"How many rows in a truth table for 3 variables?",shortAnswer:"2³ = 8 rows.",explanation:"Each variable has 2 possibilities. For n variables, 2ⁿ rows cover all combinations.",hint:"Exponential growth.",level:"moderate",codeExample:"// For p,q,r: 8 combinations from TTT to FFF"},
  {question:"Is logical OR inclusive or exclusive?",shortAnswer:"Inclusive — allows both operands to be true.",explanation:"In everyday language, 'or' can be ambiguous. Logic uses inclusive OR: p ∨ q is true when p=true, q=true, or both.",hint:"Inclusive OR includes the 'both' case.",level:"moderate",codeExample:"// Inclusive: 'You can have coffee or tea' includes possibility of both"},
  {question:"What is the result of ~(p ∧ q) when p=T, q=T?",shortAnswer:"False (F).",explanation:"p∧q is true, so its negation is false.",hint:"First evaluate inside parentheses.",level:"basic",codeExample:"let p=true,q=true; let result=!(p&&q); // false"},
  {question:"What is the result of (~p) ∨ q when p=F, q=F?",shortAnswer:"True (T).",explanation:"~p is true (since p false), so true ∨ false = true.",hint:"Negation first, then OR.",level:"moderate",codeExample:"let p=false,q=false; let result=(!p)||q; // true"},
  {question:"Which operator has highest precedence: ~, ∧, ∨?",shortAnswer:"NOT (~) has highest precedence, then AND (∧), then OR (∨).",explanation:"Just like arithmetic: NOT is like unary minus, AND like multiplication, OR like addition.",hint:"Remember: NOT before AND before OR.",level:"moderate",codeExample:"// ~p ∧ q means (~p) ∧ q, not ~(p ∧ q)"},
  {question:"What is the difference between ∧ and ∨?",shortAnswer:"∧ requires all true; ∨ requires at least one true.",explanation:"They are duals: De Morgan's laws relate them.",hint:"AND is stricter.",level:"basic",codeExample:"// AND: both must be true\n// OR: at least one must be true"},
  {question:"How does short-circuit evaluation work for && in programming?",shortAnswer:"If left operand is false, right operand is not evaluated.",explanation:"Because false ∧ anything = false. This can prevent errors and improve performance.",hint:"Used in conditions like if (obj && obj.property).",level:"moderate",codeExample:"if (user && user.isLoggedIn) { /* safe */ }"},
  {question:"What does p ∨ ~p evaluate to?",shortAnswer:"Always true (tautology).",explanation:"Law of excluded middle: a proposition is either true or its negation is true.",hint:"Always true, regardless of p.",level:"moderate",codeExample:"let alwaysTrue = p || !p; // true for any p"},
  {question:"What does p ∧ ~p evaluate to?",shortAnswer:"Always false (contradiction).",explanation:"A proposition cannot be both true and false at the same time.",hint:"Always false.",level:"moderate",codeExample:"let alwaysFalse = p && !p; // false for any p"},
  {question:"How do you express 'p but q' in logic?",shortAnswer:"p ∧ q (same as 'and').",explanation:"'But' indicates contrast but logically means conjunction.",hint:"Just use AND.",level:"basic",codeExample:"// 'It's raining but cold' → raining ∧ cold"},
  {question:"What is the negation of p ∧ q?",shortAnswer:"~p ∨ ~q (De Morgan's law).",explanation:"Not (p and q) is equivalent to (not p) or (not q).",hint:"Flip AND to OR and negate each.",level:"moderate",codeExample:"let deMorgan = !(p && q) === (!p || !q); // always true"},
  {question:"What is the negation of p ∨ q?",shortAnswer:"~p ∧ ~q (De Morgan's law).",explanation:"Not (p or q) is equivalent to (not p) and (not q).",hint:"Flip OR to AND and negate each.",level:"moderate",codeExample:"let deMorgan = !(p || q) === (!p && !q);"},
  {question:"In programming, what is the difference between & and && (in C/Java)?",shortAnswer:"& is bitwise AND; && is logical AND (short-circuit).",explanation:"Logical operators work on booleans; bitwise on integer bits. Use && for conditions.",hint:"For true/false logic, use &&, ||, !.",level:"expert",codeExample:"if (x > 0 && y < 10) // logical"},
  {question:"Can logical operators be chained like p ∧ q ∧ r?",shortAnswer:"Yes, but it's left-associative: (p ∧ q) ∧ r.",explanation:"AND and OR are associative, so parentheses don't change meaning for same operator.",hint:"All same operator, order doesn't matter.",level:"basic",codeExample:"let allTrue = p && q && r; // true only if all true"},
  {question:"What does the truth table for p ∨ q look like?",shortAnswer:"Three T rows, one F row (when both false).",explanation:"T,T→T; T,F→T; F,T→T; F,F→F.",hint:"Only false when both false.",level:"basic",codeExample:"// Truth table rows: TT, TF, FT, FF"},
  {question:"How do you represent 'neither p nor q' in logic?",shortAnswer:"~p ∧ ~q.",explanation:"'Neither A nor B' means not A and not B.",hint:"Both are false.",level:"moderate",codeExample:"let neither = !p && !q; // true when both false"},
  {question:"What is the result of (T ∧ F) ∨ T?",shortAnswer:"True (T).",explanation:"T∧F = F; F ∨ T = T.",hint:"Evaluate step by step.",level:"basic",codeExample:"let result = (true && false) || true; // true"},
  {question:"Why does p ∧ (q ∨ r) differ from (p ∧ q) ∨ r?",shortAnswer:"Different grouping changes meaning; AND has higher precedence but parentheses matter.",explanation:"Without parentheses, p ∧ q ∨ r is ambiguous. Always use parentheses.",hint:"Parentheses are not optional when mixing operators.",level:"moderate",codeExample:"// Different truth tables"},
  {question:"What is the result of ~(T ∨ F)?",shortAnswer:"False (F).",explanation:"T∨F = T; ~T = F.",hint:"OR first, then NOT.",level:"basic",codeExample:"let result = !(true || false); // false"},
  {question:"How many rows in a truth table for 4 variables?",shortAnswer:"16 rows.",explanation:"2⁴ = 16. Each variable doubles the rows.",hint:"Exponential.",level:"moderate",codeExample:"// 4 vars → 16 combinations"},
  {question:"What does the logical operator 'NOR' mean?",shortAnswer:"NOT OR: ~(p ∨ q).",explanation:"NOR is true only when both p and q are false.",hint:"It's the negation of OR.",level:"moderate",codeExample:"let nor = !(p || q); // true only if both false"},
  {question:"Can you express XOR (exclusive OR) using ∧, ∨, ~?",shortAnswer:"Yes: (p ∨ q) ∧ ~(p ∧ q).",explanation:"XOR is true when exactly one is true.",hint:"At least one but not both.",level:"expert",codeExample:"let xor = (p || q) && !(p && q);"},
  {question:"What is the precedence of NOT compared to relational operators like >?",shortAnswer:"In programming, ! has higher precedence than relational operators, but use parentheses.",explanation:"!x > 5 means (!x) > 5 (type conversion). Usually you want !(x > 5).",hint:"Use parentheses for clarity.",level:"expert",codeExample:"if (!(age >= 18)) // correct\nif (!age >= 18) // wrong (type coercion)"},
  {question:"What is the dual of an expression?",shortAnswer:"Swap ∧ with ∨ and T with F (and keep ~).",explanation:"Duality principle: if an expression is a tautology, its dual is a contradiction.",hint:"Useful for deriving laws.",level:"expert",codeExample:"Dual of p ∧ q is p ∨ q"},
  {question:"How do you simplify (p ∧ q) ∨ (p ∧ ~q)?",shortAnswer:"p (absorption).",explanation:"Factor out p: p ∧ (q ∨ ~q) = p ∧ T = p.",hint:"Distribution and complement.",level:"expert",codeExample:"let simplified = p; // (p&&q)||(p&&!q) = p"},
  {question:"What is the result of (F ∨ F) ∧ (T ∨ F)?",shortAnswer:"False (F).",explanation:"F∨F=F; T∨F=T; F∧T=F.",hint:"Evaluate each parenthesis.",level:"basic",codeExample:"let result = (false||false) && (true||false); // false"}
];

export default questions;