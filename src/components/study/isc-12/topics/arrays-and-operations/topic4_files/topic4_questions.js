const questions = [
    { question: "What is a 2D array?", shortAnswer: "An array of arrays, representing a grid of rows and columns.", explanation: "It stores data in a matrix form, accessed using two indices: one for row, one for column.", hint: "Think of a spreadsheet.", level: "basic", codeExample: "int[][] matrix = new int[3][4];" },
    { question: "How do you declare a 2D array in Java?", shortAnswer: "int[][] arr; or int arr[][];", explanation: "The first bracket pair represents rows, the second represents columns. int[][] arr = new int[3][4];", hint: "Two sets of brackets.", level: "basic", codeExample: "int[][] matrix = new int[5][5];" },
    { question: "What is the default value of elements in a 2D int array?", shortAnswer: "0", explanation: "Like 1D arrays, primitive int elements are initialized to 0.", hint: "Same as 1D arrays.", level: "basic", codeExample: "int[][] arr = new int[2][2]; // all zeros" },
    { question: "How do you access an element at row 2, column 3 in a 2D array named 'grid'?", shortAnswer: "grid[1][2] (zero-based indexing).", explanation: "Rows and columns start at 0. So row 2 is index 1, column 3 is index 2.", hint: "Zero-based indexing.", level: "basic", codeExample: "int value = grid[1][2];" },
    { question: "What does grid.length return?", shortAnswer: "Number of rows.", explanation: "grid.length gives the length of the outer array, which is the number of rows.", hint: "Outer dimension.", level: "basic", codeExample: "int rows = grid.length;" },
    { question: "How do you get the number of columns in a rectangular 2D array?", shortAnswer: "grid[0].length", explanation: "Assuming all rows have the same length, the first row's length gives the column count.", hint: "Check first row's length.", level: "basic", codeExample: "int cols = grid[0].length;" },
    { question: "What is a jagged array?", shortAnswer: "A 2D array where rows can have different lengths.", explanation: "In Java, you can create an array of arrays where each inner array has its own size.", hint: "Not all rows equal.", level: "intermediate", codeExample: "int[][] jagged = new int[3][]; jagged[0] = new int[2]; jagged[1] = new int[4];" },
    { question: "How is a 2D array stored in memory in Java?", shortAnswer: "As an array of references to 1D arrays.", explanation: "The outer array holds references to each row array. The rows are separate objects.", hint: "Array of arrays.", level: "expert", codeExample: "// matrix[i] is a reference to a 1D array" },
    { question: "Can a 2D array have rows of different lengths? If yes, what is it called?", shortAnswer: "Yes, it's called a jagged or ragged array.", explanation: "Java allows each inner array to be created independently, so they can have different lengths.", hint: "Not all rows equal length.", level: "intermediate", codeExample: "int[][] jagged = {{1,2},{3,4,5},{6}};" },
    { question: "What happens if you try to access grid[3][0] in a 2x2 array?", shortAnswer: "ArrayIndexOutOfBoundsException.", explanation: "Row indices go from 0 to rows-1. For 2 rows, valid indices are 0 and 1. Index 3 is out of bounds.", hint: "Out of range.", level: "basic", codeExample: "// Exception thrown" },
    { question: "How do you initialize a 2D array with specific values without using loops?", shortAnswer: "Use an array initializer with nested braces.", explanation: "int[][] matrix = {{1,2},{3,4},{5,6}};", hint: "Static initialization.", level: "basic", codeExample: "int[][] arr = {{1,2},{3,4}};" },
    { question: "What is the output of System.out.println(matrix) for a 2D array?", shortAnswer: "A cryptic string like [[I@15db9742", explanation: "2D arrays don't override toString(). Use Arrays.deepToString(matrix) for readable output.", hint: "Use deepToString.", level: "basic", codeExample: "System.out.println(Arrays.deepToString(matrix));" },
    { question: "How do you copy a 2D array (deep copy)?", shortAnswer: "Loop through rows and copy each row individually.", explanation: "Using clone() on the outer array only copies references (shallow copy). For deep copy, copy each row array.", hint: "Copy row by row.", level: "intermediate", codeExample: "int[][] copy = new int[original.length][]; for (int i=0; i<original.length; i++) copy[i] = original[i].clone();" },
    { question: "What is the time complexity of accessing an element in a 2D array?", shortAnswer: "O(1) constant time.", explanation: "Access is based on index calculation, independent of array size.", hint: "Random access.", level: "basic", codeExample: "// Direct addressing" },
    { question: "Can you have a 3D array in Java? If yes, how?", shortAnswer: "Yes, int[][][] arr = new int[2][3][4];", explanation: "Java supports multidimensional arrays of any depth. A 3D array is an array of 2D arrays.", hint: "More dimensions.", level: "intermediate", codeExample: "int[][][] cube = new int[5][5][5];" },
    { question: "What is the difference between int[][] arr = new int[3][4] and int[][] arr = new int[3][];", shortAnswer: "First creates a rectangular 3x4 matrix; second creates a jagged array with null rows.", explanation: "In the second, you must allocate each row separately before use.", hint: "Allocation differences.", level: "intermediate", codeExample: "int[][] arr = new int[3][]; arr[0] = new int[2];" },
    { question: "How do you find the number of rows and columns in a 2D array passed to a method?", shortAnswer: "rows = matrix.length; cols = matrix[0].length (if rectangular).", explanation: "Use the array's length property. For safety, check if matrix is not null and rows > 0 before accessing matrix[0].", hint: "Length property.", level: "intermediate", codeExample: "if (matrix != null && matrix.length > 0) { int cols = matrix[0].length; }" },
    { question: "What is a common use case for 2D arrays in real life?", shortAnswer: "Spreadsheets, images (pixels), game boards, distance matrices.", explanation: "Any grid-like data structure naturally fits a 2D array.", hint: "Grid data.", level: "basic", codeExample: "// Tic-tac-toe board, chessboard" },
    { question: "Can you store different data types in the same 2D array?", shortAnswer: "No, arrays are homogeneous.", explanation: "All elements must be of the same declared type. Use Object[][] if you need mixed types, but type safety is lost.", hint: "Same type only.", level: "basic", codeExample: "// Not allowed: int and String together" },
    { question: "How do you iterate over all elements of a 2D array?", shortAnswer: "Use nested for loops: outer for rows, inner for columns.", explanation: "Standard row-major traversal. You can also use nested enhanced for loops.", hint: "Nested loops.", level: "basic", codeExample: "for (int i=0; i<rows; i++) for (int j=0; j<cols; j++) process(matrix[i][j]);" },
    { question: "What is the memory overhead of a 2D array compared to a 1D array of same total elements?", shortAnswer: "Higher due to separate row objects and reference array.", explanation: "Each row is a separate object with its own header, plus the outer array stores references. A flattened 1D array is more memory efficient.", hint: "More objects.", level: "expert", codeExample: "// 2D: overhead of (rows+1) objects" },
    { question: "How do you pass a 2D array to a method?", shortAnswer: "Use the type int[][] as parameter.", explanation: "Methods can accept 2D arrays. The array is passed by reference.", hint: "Reference passing.", level: "basic", codeExample: "public void process(int[][] matrix) { ... }" },
    { question: "What is the result of matrix[0].length if matrix is a 2D array with no rows (matrix.length == 0)?", shortAnswer: "ArrayIndexOutOfBoundsException because matrix[0] does not exist.", explanation: "If there are no rows, you cannot access row 0. Always check rows > 0 before accessing matrix[0].", hint: "Empty array trap.", level: "intermediate", codeExample: "if (matrix.length > 0) { int cols = matrix[0].length; }" },
    { question: "Can you use the enhanced for loop with a 2D array? How?", shortAnswer: "Yes, by iterating over rows, then over columns.", explanation: "for (int[] row : matrix) { for (int val : row) { ... } }", hint: "Nested for-each.", level: "intermediate", codeExample: "for (int[] row : grid) for (int num : row) System.out.print(num + \" \");" },
    { question: "What is the maximum number of dimensions an array can have in Java?", shortAnswer: "255 dimensions (theoretically), but practically 2-3 are used.", explanation: "Java specification allows up to 255 dimensions, but beyond 3 is rarely needed.", hint: "Very high limit.", level: "expert", codeExample: "int[][][][][]... up to 255" },
    { question: "How do you create a 2D array with variable row lengths (jagged) using an initializer?", shortAnswer: "Use nested braces with different inner lengths.", explanation: "int[][] jagged = {{1,2},{3,4,5},{6}};", hint: "Different inner sizes.", level: "intermediate", codeExample: "int[][] jagged = {{1,2},{3,4,5}};" },
    { question: "What is the difference between matrix.length and matrix[0].length?", shortAnswer: "matrix.length is rows; matrix[0].length is columns (if rectangular).", explanation: "First gives number of rows (outer array length). Second gives length of the first row, which is the number of columns in a rectangular matrix.", hint: "Rows vs columns.", level: "basic", codeExample: "int rows = matrix.length; int cols = matrix[0].length;" },
    { question: "How do you fill a 2D array with a specific value (e.g., 0) efficiently?", shortAnswer: "Use nested loops or Arrays.fill() on each row.", explanation: "For each row, use Arrays.fill(row, value).", hint: "Row-by-row fill.", level: "intermediate", codeExample: "for (int[] row : matrix) Arrays.fill(row, 0);" },
    { question: "What is a common beginner mistake when declaring a 2D array?", shortAnswer: "Forgetting to allocate inner arrays.", explanation: "Declaring int[][] arr = new int[3][]; leaves rows null. Must allocate each row: arr[0] = new int[4];", hint: "Null rows.", level: "basic", codeExample: "// NullPointerException if row not allocated" },
    { question: "How does Java treat a 2D array for garbage collection?", shortAnswer: "Each row array is eligible for GC when no references point to it.", explanation: "If the outer array is null, all rows become unreachable and can be garbage collected.", hint: "Reference chain.", level: "expert", codeExample: "matrix = null; // rows may be collected" }
];

export default questions;