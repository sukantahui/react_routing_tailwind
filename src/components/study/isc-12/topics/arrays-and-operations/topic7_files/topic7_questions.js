const questions = [
    { question: "What is row-major traversal of a 2D array?", shortAnswer: "Visiting elements row by row (outer loop rows, inner loop columns).", explanation: "All columns of first row, then all columns of second row, etc.", hint: "Like reading a book.", level: "basic", codeExample: "for (int i=0; i<rows; i++) for (int j=0; j<cols; j++) process(matrix[i][j]);" },
    { question: "What is column-major traversal?", shortAnswer: "Visiting elements column by column (outer loop columns, inner loop rows).", explanation: "All rows of first column, then all rows of second column, etc.", hint: "Vertical reading.", level: "basic", codeExample: "for (int j=0; j<cols; j++) for (int i=0; i<rows; i++) process(matrix[i][j]);" },
    { question: "Why is row-major traversal faster than column-major in Java?", shortAnswer: "Due to spatial locality and CPU caching (rows are stored contiguously).", explanation: "Java stores each row as a contiguous block; row-major accesses memory sequentially.", hint: "Cache friendliness.", level: "expert", codeExample: "// Row-wise: sequential memory access" },
    { question: "How do you traverse a 2D array using enhanced for loop?", shortAnswer: "for (int[] row : matrix) for (int val : row) { ... }", explanation: "Outer loop iterates over rows, inner over elements.", hint: "Nested for-each.", level: "intermediate", codeExample: "for (int[] row : arr) for (int x : row) System.out.print(x + \" \");" },
    { question: "What is the time complexity of traversing an m×n matrix?", shortAnswer: "O(m × n)", explanation: "You visit each of the m*n elements exactly once.", hint: "Linear in total elements.", level: "basic", codeExample: "// m*n operations" },
    { question: "Can you traverse a matrix in reverse row-major order?", shortAnswer: "Yes, start from last row and last column and decrement.", explanation: "for (int i=rows-1; i>=0; i--) for (int j=cols-1; j>=0; j--)", hint: "Reverse loops.", level: "intermediate", codeExample: "for (int i=rows-1; i>=0; i--) for (int j=cols-1; j>=0; j--)" },
    { question: "How do you traverse only the boundary elements of a matrix?", shortAnswer: "Use loops for top row, right column, bottom row, left column.", explanation: "Handle single row/column edge cases.", hint: "Perimeter traversal.", level: "expert", codeExample: "// top row: j=0 to cols-1; right col: i=1 to rows-1; bottom row: j=cols-2 down to 0; left col: i=rows-2 down to 1" },
    { question: "What is the difference between matrix.length and matrix[0].length?", shortAnswer: "matrix.length is number of rows; matrix[0].length is number of columns (if rectangular).", explanation: "First gives outer array size, second gives size of first row.", hint: "Rows vs columns.", level: "basic", codeExample: "int rows = matrix.length; int cols = matrix[0].length;" },
    { question: "How do you traverse a jagged array safely?", shortAnswer: "Use matrix[i].length in inner loop condition.", explanation: "Each row may have different length.", hint: "Row length varies.", level: "intermediate", codeExample: "for (int i=0; i<matrix.length; i++) for (int j=0; j<matrix[i].length; j++)" },
    { question: "What is the space complexity of matrix traversal?", shortAnswer: "O(1) additional space (excluding the matrix).", explanation: "Only loop variables are used.", hint: "Constant extra memory.", level: "basic", codeExample: "// No extra arrays" },
    { question: "Can you modify matrix elements during traversal?", shortAnswer: "Yes, using indexed loops (not enhanced for-each for primitives).", explanation: "matrix[i][j] = newValue works. Enhanced for-each gives a copy for primitives.", hint: "Use index loop to modify.", level: "intermediate", codeExample: "for (int i=0; i<rows; i++) for (int j=0; j<cols; j++) matrix[i][j] *= 2;" },
    { question: "What happens if you use column-major traversal on a jagged array?", shortAnswer: "It may cause ArrayIndexOutOfBoundsException if rows have different lengths.", explanation: "Column-major assumes all rows have same number of columns.", hint: "Not safe for jagged.", level: "expert", codeExample: "// Exception if rows have different lengths" },
    { question: "How do you traverse a matrix diagonally (main diagonal)?", shortAnswer: "Loop i from 0 to min(rows,cols)-1 and access matrix[i][i].", explanation: "Only square matrices or rectangular up to min dimension.", hint: "i == j.", level: "intermediate", codeExample: "for (int i=0; i<rows && i<cols; i++) System.out.println(matrix[i][i]);" },
    { question: "What is the output of row-wise traversal for a 2x3 matrix with values 1..6?", shortAnswer: "1 2 3 then 4 5 6", explanation: "Row 0: 1,2,3; Row 1: 4,5,6.", hint: "Sequential rows.", level: "basic", codeExample: "// 1 2 3\n4 5 6" },
    { question: "What is the output of column-wise traversal for a 2x3 matrix with values 1..6?", shortAnswer: "1 4 then 2 5 then 3 6", explanation: "Col 0: 1,4; Col 1: 2,5; Col 2: 3,6.", hint: "Vertical columns.", level: "basic", codeExample: "// 1 4\n2 5\n3 6" },
    { question: "How do you traverse a matrix in spiral order?", shortAnswer: "Use four boundaries (top, bottom, left, right) and reduce them after each cycle.", explanation: "Advanced traversal pattern.", hint: "Boundary variables.", level: "expert", codeExample: "while (top <= bottom && left <= right) { // traverse top row, right col, bottom row, left col }" },
    { question: "Can you use stream API to traverse a 2D array?", shortAnswer: "Yes, using Arrays.stream(matrix).flatMapToInt(Arrays::stream).", explanation: "Converts 2D to 1D stream.", hint: "Stream flattening.", level: "expert", codeExample: "Arrays.stream(matrix).flatMapToInt(Arrays::stream).forEach(System.out::println);" },
    { question: "What is the performance difference between row-wise and column-wise for a 10000x10000 matrix?", shortAnswer: "Row-wise can be 5-10x faster.", explanation: "Cache misses in column-wise cause dramatic slowdown.", hint: "Cache effects.", level: "expert", codeExample: "// Test with performance comparison" },
    { question: "How do you traverse a matrix in zigzag (snake) pattern?", shortAnswer: "Traverse row 0 left to right, row 1 right to left, row 2 left to right, etc.", explanation: "Use a flag or check row % 2.", hint: "Alternating direction.", level: "expert", codeExample: "for (int i=0; i<rows; i++) { if (i%2==0) for j=0..cols-1; else for j=cols-1..0; }" },
    { question: "What is the advantage of using enhanced for loop over indexed loop for traversal?", shortAnswer: "Cleaner, less error-prone (no off-by-one), and works for any Iterable.", explanation: "Best for read-only operations.", hint: "Readability.", level: "basic", codeExample: "// No index variables to manage" },
    { question: "How do you traverse only the upper triangular part of a square matrix?", shortAnswer: "for (int i=0; i<rows; i++) for (int j=i; j<cols; j++)", explanation: "Elements where row index <= column index.", hint: "Upper triangle.", level: "intermediate", codeExample: "// Includes diagonal" },
    { question: "What is the memory access pattern of row-major traversal?", shortAnswer: "Sequential, stride-1 access.", explanation: "Each element is next to the previous in memory.", hint: "Contiguous.", level: "expert", codeExample: "// Good cache utilization" },
    { question: "How do you traverse a matrix in reverse column-major order?", shortAnswer: "for (int j=cols-1; j>=0; j--) for (int i=rows-1; i>=0; i--)", explanation: "Start from bottom-right, go up columns, then left.", hint: "Reverse vertical.", level: "intermediate", codeExample: "// Last column first, from bottom to top" },
    { question: "What is the difference between traversal and iteration?", shortAnswer: "Traversal means visiting all elements; iteration is the process of looping.", explanation: "Often used interchangeably.", hint: "Subtle semantics.", level: "basic", codeExample: "// Loops perform traversal" },
    { question: "How do you traverse a 2D array of objects and call a method on each?", shortAnswer: "Use nested loops and call the method on each element.", explanation: "matrix[i][j].methodName().", hint: "Method invocation.", level: "intermediate", codeExample: "for (int i=0; i<rows; i++) for (int j=0; j<cols; j++) matrix[i][j].display();" },
    { question: "What is the fastest way to traverse a large 2D array for summation?", shortAnswer: "Row-wise traversal with primitive loops, unrolling if needed.", explanation: "JIT optimizes simple loops.", hint: "Row-major.", level: "expert", codeExample: "// Simple row-wise loop is best" },
    { question: "Can you traverse a matrix using recursion?", shortAnswer: "Yes, but not recommended for large matrices due to stack overflow.", explanation: "Recursive function can process cell (i,j) and call next.", hint: "Stack depth.", level: "expert", codeExample: "void traverse(int[][] m, int i, int j) { if (i>=rows) return; if (j>=cols) { traverse(m,i+1,0); return; } process(m[i][j]); traverse(m,i,j+1); }" },
    { question: "How do you traverse a matrix in a 'boustrophedon' (alternating direction) pattern?", shortAnswer: "Same as snake pattern: left-to-right, then right-to-left.", explanation: "Like an ox plowing a field.", hint: "Snake pattern.", level: "expert", codeExample: "// See zigzag traversal" },
    { question: "What is the effect of modifying a matrix while traversing it?", shortAnswer: "Safe as long as you don't change dimensions or indices.", explanation: "Changing values is fine; adding/removing rows is not possible.", hint: "Value modification OK.", level: "intermediate", codeExample: "matrix[i][j] = matrix[i][j] * 2; // valid" },
    { question: "How do you traverse a matrix using a single loop (flattened index)?", shortAnswer: "for (int i=0; i<rows*cols; i++) { int r = i/cols; int c = i%cols; process(matrix[r][c]); }", explanation: "Treat 2D as 1D with index calculation.", hint: "Flattening.", level: "expert", codeExample: "// Useful for some algorithms" }
];

export default questions;