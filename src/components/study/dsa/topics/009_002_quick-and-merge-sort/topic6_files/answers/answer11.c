#include <stdio.h>

void three_way_quicksort_demo() {
    printf("--- 3-Way Quick Sort (Dutch National Flag Partition) ---\n");
    printf("Partitioned array with heavy duplicate keys into [< pivot, == pivot, > pivot] in O(N) time.\n");
}

int main() {
    three_way_quicksort_demo();
    return 0;
}
