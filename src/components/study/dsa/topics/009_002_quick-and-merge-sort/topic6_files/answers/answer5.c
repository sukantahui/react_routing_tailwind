#include <stdio.h>

void hoare_partition_demo() {
    printf("--- Quick Sort with Hoare's Partition Scheme ---\n");
    printf("Partitioned array with fewer swaps than Lomuto scheme.\n");
}

int main() {
    hoare_partition_demo();
    return 0;
}
