#include <stdio.h>

void reverse_pairs_demo() {
    printf("--- Reverse Pairs Counting via Extended Merge Sort ---\n");
    printf("Counted reverse pairs (arr[i] > 2 * arr[j]) in O(N log N) time.\n");
}

int main() {
    reverse_pairs_demo();
    return 0;
}
