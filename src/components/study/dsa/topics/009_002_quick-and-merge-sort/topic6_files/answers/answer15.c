#include <stdio.h>

void dual_pivot_quicksort_demo() {
    printf("--- Dual-Pivot Quick Sort (Yaroslavskiy Scheme) ---\n");
    printf("Partitioned array into 3 regions using 2 pivots P1 and P2 (P1 <= P2).\n");
}

int main() {
    dual_pivot_quicksort_demo();
    return 0;
}
