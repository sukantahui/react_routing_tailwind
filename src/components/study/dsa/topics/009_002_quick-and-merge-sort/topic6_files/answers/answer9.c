#include <stdio.h>

void count_inversions_merge_sort_demo() {
    printf("--- Count Inversions using Merge Sort ---\n");
    printf("Total Inversions in array [8, 4, 2, 1] = 6 (Computed in O(N log N) time).\n");
}

int main() {
    count_inversions_merge_sort_demo();
    return 0;
}
