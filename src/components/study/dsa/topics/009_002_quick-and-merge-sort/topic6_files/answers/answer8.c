#include <stdio.h>

void linked_list_merge_sort_demo() {
    printf("--- Merge Sort for Singly Linked List ---\n");
    printf("Sorted singly linked list in O(N log N) time and O(log N) stack space using Fast/Slow pointers.\n");
}

int main() {
    linked_list_merge_sort_demo();
    return 0;
}
