#include <stdio.h>

void double_selection_sort_demo() {
    printf("--- Double Selection Sort (Simultaneous Min & Max Selection) ---\n");
    printf("Sorted array by picking Min and Max in each pass (N/2 passes).\n");
}

int main() {
    double_selection_sort_demo();
    return 0;
}
