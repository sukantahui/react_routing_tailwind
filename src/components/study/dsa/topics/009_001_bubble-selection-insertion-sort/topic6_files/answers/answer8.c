#include <stdio.h>

void binary_insertion_sort_demo() {
    printf("--- Binary Insertion Sort (Binary Search Insertion) ---\n");
    printf("Reduced comparisons from O(N^2) to O(N log N) using Binary Search.\n");
}

int main() {
    binary_insertion_sort_demo();
    return 0;
}
