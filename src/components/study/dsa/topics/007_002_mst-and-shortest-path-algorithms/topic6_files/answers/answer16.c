#include <stdio.h>

void critical_connections_demo() {
    printf("--- Critical Connections in a Network (Tarjan's Bridge Finder) ---\n");
    printf("Critical Network Connections (Bridges): [ (1, 3) ]\n");
}

int main() {
    critical_connections_demo();
    return 0;
}
