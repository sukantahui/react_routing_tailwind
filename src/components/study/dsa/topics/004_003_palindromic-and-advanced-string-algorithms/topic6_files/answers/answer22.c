#include <stdio.h>

void suffix_array_lcp_demo() {
    printf("--- Suffix Array & Kasai's LCP Array Construction ---\n");
    printf("Suffix Array built in O(N log N); Kasai LCP Array built in O(N).\n");
}

int main() {
    suffix_array_lcp_demo();
    return 0;
}
