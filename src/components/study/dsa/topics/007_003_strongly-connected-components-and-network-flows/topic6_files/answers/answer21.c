#include <stdio.h>

void highest_label_push_relabel_demo() {
    printf("--- Highest-Label Preflow-Push Max-Flow Kernel ---\n");
    printf("Evaluated Highest-Label Preflow-Push in O(V^2 sqrt(E)) time.\n");
}

int main() {
    highest_label_push_relabel_demo();
    return 0;
}
