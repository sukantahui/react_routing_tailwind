#include <stdio.h>

void push_relabel_demo() {
    printf("--- Push-Relabel / Preflow-Push Algorithm ---\n");
    printf("Maximum Flow Value (Push-Relabel) = 23 (Completed in O(V^3) time).\n");
}

int main() {
    push_relabel_demo();
    return 0;
}
