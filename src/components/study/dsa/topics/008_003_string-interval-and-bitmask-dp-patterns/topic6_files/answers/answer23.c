#include <stdio.h>

void hirschberg_lcs_demo() {
    printf("--- Hirschberg's Linear Space LCS Algorithm ---\n");
    printf("Computed LCS string alignment in O(M * N) time and linear O(M + N) auxiliary space.\n");
}

int main() {
    hirschberg_lcs_demo();
    return 0;
}
