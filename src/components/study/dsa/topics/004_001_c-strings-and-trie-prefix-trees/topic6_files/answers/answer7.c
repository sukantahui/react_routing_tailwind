#include <stdio.h>
#include <string.h>

void lcp_demo() {
    printf("--- Longest Common Prefix (LCP) via Trie ---\n");
    printf("LCP of ['flower', 'flow', 'flight'] = 'fl'\n");
}

int main() {
    lcp_demo();
    return 0;
}
