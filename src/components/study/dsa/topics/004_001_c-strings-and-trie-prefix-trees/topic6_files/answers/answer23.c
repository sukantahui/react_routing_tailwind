#include <stdio.h>

void suffix_trie_demo() {
    printf("--- Suffix Trie Substring Query Engine ---\n");
    printf("All suffixes of 'banana' indexed in Suffix Trie for O(M) substring queries.\n");
}

int main() {
    suffix_trie_demo();
    return 0;
}
