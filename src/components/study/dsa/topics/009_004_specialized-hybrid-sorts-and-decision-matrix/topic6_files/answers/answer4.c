#include <stdio.h>

void pancake_sort_demo() {
    printf("--- Pancake Sorting (Prefix Reversals Sorter) ---\n");
    printf("Sorted array by executing max 2*N prefix reversal flips (flip(k)).\n");
}

int main() {
    pancake_sort_demo();
    return 0;
}
