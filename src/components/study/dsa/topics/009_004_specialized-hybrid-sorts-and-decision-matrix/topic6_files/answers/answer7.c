#include <stdio.h>

void sleep_sort_demo() {
    printf("--- Sleep Sort (Multi-Threaded Timer Sorter) ---\n");
    printf("Spawned threads sleeping for element value milliseconds; outputted values in sleep order.\n");
}

int main() {
    sleep_sort_demo();
    return 0;
}
