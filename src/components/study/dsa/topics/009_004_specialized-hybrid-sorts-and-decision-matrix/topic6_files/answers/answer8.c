#include <stdio.h>

void stooge_sort_demo() {
    printf("--- Stooge Sort (Recursive 2/3 Subarray Sorter) ---\n");
    printf("Sorted array recursively in O(N^2.7) time using 2/3 overlap swaps.\n");
}

int main() {
    stooge_sort_demo();
    return 0;
}
