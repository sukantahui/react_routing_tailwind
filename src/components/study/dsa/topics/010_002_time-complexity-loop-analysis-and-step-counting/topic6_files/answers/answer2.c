#include <stdio.h>

void independent_nested_loop_demo() {
    printf("--- Independent Nested Loops Step Counter ---\n");
    printf("Independent nested loops executed N * M steps (O(N * M) time complexity).\n");
}

int main() {
    independent_nested_loop_demo();
    return 0;
}
