#include <stdio.h>

void multi_var_matrix_loop_demo() {
    printf("--- Multi-Variable Matrix Loop Analyzer ---\n");
    printf("Evaluated 2D grid matrix loop for i=0..R-1, j=0..C-1: R * C steps (O(R * C) time complexity).\n");
}

int main() {
    multi_var_matrix_loop_demo();
    return 0;
}
