#include <stdio.h>

void triangular_nested_loop_demo() {
    printf("--- Triangular Nested Loop Step Formula ---\n");
    printf("Evaluated triangular loop for j=i..N-1: N(N+1)/2 steps (O(N^2) time complexity).\n");
}

int main() {
    triangular_nested_loop_demo();
    return 0;
}
