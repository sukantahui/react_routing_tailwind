#include <stdio.h>

void single_loop_counter_demo() {
    printf("--- Single Loop Step Counter ---\n");
    printf("Single loop for i=0..N-1 executed EXACTLY N steps (O(N) time complexity).\n");
}

int main() {
    single_loop_counter_demo();
    return 0;
}
