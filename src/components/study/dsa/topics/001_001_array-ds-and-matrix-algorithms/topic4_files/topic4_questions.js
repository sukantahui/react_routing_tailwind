const questions = [
  {
    id: 1,
    question: "What constraint must be met before multiplying Matrix A (size m x n) and Matrix B (size p x q)?",
    options: ["n must equal p (Columns of A = Rows of B)", "m must equal q", "Both matrices must be square", "Elements must be positive"],
    answer: "n must equal p (Columns of A = Rows of B)",
    explanation: "Matrix multiplication requires that the number of columns in Matrix A equals the number of rows in Matrix B."
  }
];

export default questions;
