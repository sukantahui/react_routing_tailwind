#include <stdio.h>

void pigeonhole_sort_demo() {
    printf("--- Pigeonhole Sort (Key-Value Range Distribution) ---\n");
    printf("Sorted array elements by distributing values into pigeonholes in O(N + Range) time.\n");
}

int main() {
    pigeonhole_sort_demo();
    return 0;
}
