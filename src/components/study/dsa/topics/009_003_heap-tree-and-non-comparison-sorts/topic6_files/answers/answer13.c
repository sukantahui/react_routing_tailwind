#include <stdio.h>

void smoothsort_demo() {
    printf("--- Smoothsort (Leonardo Heap Adaptive O(N) Sorting Algorithm) ---\n");
    printf("Sorted array in O(N) time for nearly-sorted inputs using Leonardo trees heap structure.\n");
}

int main() {
    smoothsort_demo();
    return 0;
}
