#include <stdio.h>

void radix_sort_demo() {
    printf("--- Radix Sort (LSD Digit Counting Sort) ---\n");
    printf("Sorted 3-digit numbers [170, 45, 75, 90, 802, 24, 2, 66] in O(D * (N + K)) time.\n");
}

int main() {
    radix_sort_demo();
    return 0;
}
