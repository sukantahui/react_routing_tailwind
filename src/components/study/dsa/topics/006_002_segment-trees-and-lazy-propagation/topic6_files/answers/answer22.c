#include <stdio.h>

void hld_segment_tree_demo() {
    printf("--- Heavy-Light Decomposition (HLD) + Segment Tree ---\n");
    printf("Executed tree path query `queryPath(u, v)` in O(log^2 N) time via HLD Segment Tree.\n");
}

int main() {
    hld_segment_tree_demo();
    return 0;
}
