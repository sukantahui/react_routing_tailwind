#include <stdio.h>

void segment_tree_beats_demo() {
    printf("--- Segment Tree Beats (Chmin & Chmax Range Operations) ---\n");
    printf("Executed range chmin update `arr[i] = min(arr[i], V)` in O(log N) amortized time.\n");
}

int main() {
    segment_tree_beats_demo();
    return 0;
}
