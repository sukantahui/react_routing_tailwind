#include <stdio.h>

void segment_tree_walk_demo() {
    printf("--- Segment Tree Walk (Binary Search on Segment Tree) ---\n");
    printf("First index with prefix sum >= 15 is Index 3 (Found in O(log N) time).\n");
}

int main() {
    segment_tree_walk_demo();
    return 0;
}
