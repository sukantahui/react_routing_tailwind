// Project: Chaining Array Methods (Map → Filter → Reduce)
// Description: This project demonstrates chaining array methods (map → filter → reduce) in JavaScript.


const numbers = [1, 2, 3, 4, 5];
const doubled = numbers.map(n => n * 2);
console.log('Original:', numbers);
console.log('Doubled:', doubled);

